# flow.html 改訂 計画（GitHub Project「Plan for Revising Workshop Materials」対応）

## ゴール
haruki-cl/3daysWorkshop-materials の Issue #1〜#9 を1件ずつ深掘りし、flow.html（および必要なら関連資料）を修正する。
各 Issue は「深掘り（現状確認・必要なら調査）→ 具体案の提示 → 適用 → 報告」の順で進める。

## 進め方
- 1 Issue ずつ着手・完了させる。曖昧な Issue は着手前に解釈を1〜2行明示する。
- 調査系（#1, #6）は claude-code-guide 等で事実確認してから反映方針を決める。
- 各 Issue 完了ごとに本ファイルの進捗を更新する。

## ステップ一覧（Issue 番号順）

### #1 claude code サブスク→API 利用への切替調査 ⏳ 着手中
- 内容: API 変更時に API 接続が遮断される問題の追及。サブスク認証と API キー認証の違いを調査し資料に反映。
- 完了条件: 遮断の原因が特定され、切替時の注意/手順が flow.html に反映されている。

### #2 テスト設計の堅牢性検証（Day3 に章追加） ✅ 完了
- 内容: Day2 のテスト設計改訂は不要。より厳重なテスト設計の章を Day3 に新設。
- 解釈（議論の末に確定）: Day3 のオプション課題 [12]（★★★）として新設。方針は「[5] のコピー（与えられた観点チェックリスト）にしない」。芯は (1) 機能レンズ=意図した仕様から期待動作表を起こし要るテストを自分で決める／(2) リスクレンズ=「壊れたら困る」で優先順位＆探索、(3) AI に実装からテストを作らせない（実装追認の罠回避）、(4) 人間=意図と判断・AI=量/機構/批判の分担。ワークショップで回せるよう圧縮版に確定: 分担表は参照図1つ、手を動かすのは1機能・1ループ（約60分）、深掘り（2機能目・カバレッジ）は任意。既存の番号体系（全体マップ・ゴール表・オプション表・TOC）にも [12] を追記して整合。
- 完了条件: Day3 にテスト設計強化の章 [12] が追加され、関連する一覧表・ナビと整合している。

### #3 資料の強調メリハリ ⬜
- 内容: 重要な点とそうでない点が同じ強調度。本当に必要な知識をさらに強調。
- 完了条件: 重要箇所の強調が視覚的に区別されている。

### #4 進捗表の粒度改善 ⬜
- 内容: 長時間の割に進捗表が粗い。もう少し細かく分割。
- 完了条件: タイムテーブル/進捗表がより細かい粒度になっている。

### #5 Claude Design 説明の改訂 ✅ クローズ（資料変更なし）
- 内容: 後で複数案(3案)を出してもらう旨を、資料通りに実装させるなら先に述べる必要がある。
- 結論: 3案前提は既に [1]（流れ step1「3案出してもらうよう伝える」・step2「3案を見比べて選ぶ」・ポイント）に記載済みで資料の欠落ではない。本質は「着手前に主催者が口頭で先に伝える」進行上の問題のため、資料は変更せず主催者側タスクとして GitHub の issue で追跡することにした。

### #6 CI/CD (Cloudflare Workers) 採用検討 ✅ 完了
- 内容: Wrangler 経由だけでなく GitHub main 連携で CI/CD 可能か検討。
- 調査結論(2026-07): 「Workers Builds」で実現可能。GitHub リポジトリを Worker に接続し指定ブランチ(既定 main)への push で自動ビルド＆デプロイ。モノレポは Root directory=backend/ で対応、Build/Deploy コマンド指定可(既定 npx wrangler deploy、npm run deploy に変更可)、非本番ブランチは versions upload でプレビュー＋PR コメント。無料枠 3,000 分/月・同時1・最大20分/build。前提: ダッシュボードの Worker 名と wrangler.jsonc の name 一致。
- 反映方針: Day 3 のオプション課題として独立させ、新設した [13] 自動デプロイ（Workers Builds）に反映済み。あわせて s07・[6] 末尾・後片付け表からも [13] へ参照を張った。フロントデプロイ [3] を含めた CI/CD 化は今回は見送り（[6] で front/back 統合後の Worker を対象とするため）。
- 完了条件: 実現可否と、採用する場合の手順/注記の反映方針が決まっている。→ 両方確定・反映済み。

### #7 バックエンド連携説明の更新 ✅ 完了
- 内容: バックエンド連携の資料が難解。もっと分かりやすく。
- 対応(A〜D): (A)[4]冒頭に「このステップでやること（全体像）」4項目を追加 (B)D1/KV とモック認証を短文化し、KV結果整合→Durable Objects の深い注意は補足へ格下げ (C)統合手順5に「2サーバー→1本・同じ住所」の平易な導入を追加 (D)CORS/オリジンの重い括弧定義を「同じ住所」に言い換えて簡素化。
- 完了条件: [4] バック作成周辺の説明が平易化されている。→ 満たした。

### #8 SonarCloud 説明の追記 ⬜
- 内容: 説明が単調。説明/見方/修正対象の見極め/Claude Code への渡し方/修正方法/skills 活用を追記。
- 完了条件: [11] セキュリティ節の SonarCloud 説明が実行可能な粒度に拡充されている。

### #9 SCREEN_SPEC.md サンプル追加 ⬜
- 内容: サンプルがないと作成に迷う。サンプル追加、または AI に作らせる道筋の提示。
- 完了条件: flow.html に SCREEN_SPEC.md のサンプルまたは生成手順が追加されている。

---

## 別タスク: flow.html ⇄ slides.html の相互リンク（章キー方式） ✅ 完了（ヘッドレス Chrome で描画・ジャンプを実確認）
- 背景: 2ファイルが独立していて「詳細は flow.html で」と言っても slides 側がどこを指すか分からない。さらに slides.html のハッシュは通し番号（`#42`）なので、スライドを1枚挿入するだけで全リンクが腐る。
- 解釈: 今回のスコープは「参照のズレ」の解消のみ。内容の二重管理（部分SSOT・突き合わせスクリプト）は別タスクとして切り出す。
- 方針: flow.html の見出し id（`s1`〜`s12`, `s0`, `s-map`, `auth-*` 等）を**両ファイル共通の章キー**として採用する。番号ではなく意味のIDで結ぶのでスライド増減に強い。
- ステップ:
  1. slides.html: `SEC2REF` テーブル＋`refFor()` を追加し、`sec` から章キーを導出。決まらないもの（divider・はじめに配下・認証の細目）にだけ `ref:` を明示 — 完了条件: cover 以外の全スライドが章キーを持つ
  2. slides.html: `#s4` のようなキーのハッシュで該当スライド群の先頭へジャンプ（既存の `#42` 番号指定は後方互換で維持）— 完了条件: `slides.html#s4` で [4] の先頭スライドが開く
  3. slides.html: HUD（下部バー）に「詳細 → flow.html#<key>」を表示。スライド本文には入れない（`fitSlide` の縮小判定に影響させないため）— 完了条件: スライド送りでリンク先が追従する
  4. flow.html: 見出しを走査して「🎞 スライド」リンクを自動挿入。`SLIDE_KEYS` に載るキーのみ描画（スライドが無い章にリンクを出さない）— 完了条件: データ追加ゼロで全該当見出しにリンクが付く
  5. flow.html: SCREEN_SPEC の h3 に `id="s2-spec"` を付与（slides の該当2枚が指す先。従来「flow.html の details 参照」と地の文だった箇所）
- 途中で追加した対応（理由付き）:
  - flow.html に `hashchange` リスナーを追加 — SPA 方式でセクションを display 制御しているため、同じタブを別ハッシュで開き直してもブラウザ標準のジャンプが効かなかった。
  - リンクの target を名前付き（`slidesdeck` / `flowdoc`）に変更 — `_blank` だとクリックのたびにタブが増える。`rel="noopener"` はターゲット名を無効化するため外した（同一オリジンの自資料のみ）。
  - `.sbody a` の色指定を追加 — slides.html に `a` のスタイルが無く、本文中リンクがブラウザ既定の青になるため。
- 完了条件: 双方向リンクが章キーで張られ、スライドを増減してもリンクが腐らない。→ 満たした。
- 確認済み: キーの三方一致（flow の見出し id 35 / slides の ref 33種 / SLIDE_KEYS 33）＝不一致ゼロ。cover 2枚以外の全 149 スライドが章キーを持つ。ヘッドレス Chrome で flow.html に 33 本のリンク挿入・`#s8` で ps-day3 が開くこと、slides.html が `#s8`→101枚目 / `#s2-spec`→37枚目 / `#42`→42枚目（後方互換）/ 不正キー→1枚目 で動くことを確認。
- 未確認: 実ブラウザでのタブ再利用挙動（名前付き target）、印刷レイアウト。
- 未スコープ（報告のみ）: 内容の二重管理（完了条件表などの部分SSOT化、同期チェックスクリプトの常設、フルSSOT）。ビルド導入は file:// 直開きを壊すため非推奨。

---

## 別タスク: レート制限＋KV の完全削除 ✅ 完了（配信ファイル・参照SVGは残存ゼロを grep で確認）
- 追加で判明: 参照SVG 2枚（frontend_backend_integration.svg・arch_local_vs_prod.svg）にも「D1 / KV」ラベルがあり D1 単独に修正済み。
- d1_vs_kv.svg は参照ゼロで孤立（ファイルは削除せず残置＝要確認）。
- 未対応（スコープ外・報告のみ）: facilitator-guide.md（生きた講師ガイド、要更新の候補）／review-findings.md・WORKSHOP_LOG.md（過去ログのため改変せず）。

---

## 別タスク: 画面テストの追加＋強制ゲート注記 ✅ 完了（grep・構造目視で確認）
- 決定事項（ユーザー指示）:
  - 画面テストは Day 2 の [5] に「1本だけ」軽く足す。[5] を複雑にしない。
  - 技術スタック（Vitest）で書ける形にする＝新フレームワーク追加なし。Vitest + @testing-library/react（jsdom）のコンポーネント/画面テスト1本、API はモック。
  - 強制ゲート（CI強制実行＋カバレッジ下限）は実装せず「注記」のみ。置き場所は [12]（[5] に入れると重くなるため）。
- ステップ:
  1. flow.html [5] に画面テスト節（軽量）＋完了の定義に1行追加
  2. flow.html [12] に強制ゲートの注記（スコープ外・SonarCloud品質ゲートに言及）
  3. slides.html の [5]・[12] 対応スライドに同内容を反映
  4. grep で整合確認（画面テスト/testing-library/ゲート注記が両ファイルに存在）
- 完了条件: flow.html と slides.html の [5] に画面テスト1本の記述、[12] にゲート注記があり、[5] が過度に重くなっていない。

---

## 別タスク: [5] revised フローの実地検証（DEMO/testAi-run） ✅ 完了（画面テスト1本 green・壊すと赤・本番ビルド通過）
- 検証結果（資料への反映候補）:
  1. **`npm test` 1回では両方走らない** — ルートに package.json が無く、backend / frontend で別々に実行が必要。flow.html の「npm test でグリーンを確認（バックの API テスト＋フロントの画面テスト）」は要修正。
  2. 既存 TEST.md に「フロントの見た目は本テストの対象外」と書かれていた → [5] を一度終えた後に足す場合は既存記述の更新が必要（今回は書き換えた）。
  3. 実装側の具体的手当て: vite.config.ts の defineConfig を `vite` → `vitest/config` に変更＋`test.environment='jsdom'`。資料はプロンプト任せで明示なし（AI がやる想定なので許容範囲）。
  4. 未確認: globals:false のため @testing-library/react の自動 cleanup が効かない可能性（1本なら無害、2本目以降で干渉の恐れ）。
  5. npm audit の high 3件は既存依存（postcss / react-router）由来。今回追加した3依存とは無関係。
- 目的: 統合した [5]（戦略→レビュー→実装に画面テストを内包）が実際に回るか、資料のプロンプトの過不足を検証する。
- 場所: /Users/ha-toge/workshop/workshop1/DEMO/testAi-run（元 DEMO/testAi は触らない）。
- 着手前に判明した検証ポイント:
  - 既存 backend/docs/TEST.md は backend 専用で「フロントの見た目は本テストの対象外」と明記 → 戦略段階で更新が必要（資料は新規作成前提の書き方）。
  - frontend には vitest 本体も testing-library も jsdom も無い（資料の記述は是正済み）。vite.config.ts に @ エイリアスあり・.npmrc に min-release-age=7。
- ステップ:
  1. TEST.md に画面テスト方針を追記（資料 STEP①戦略）
  2. presence レビュー（資料 STEP②）
  3. frontend に vitest/@testing-library/react/jsdom を追加＋設定＋画面テスト1本（資料 STEP③）
  4. npm test green 確認、資料の過不足を洗い出して報告
- 完了条件: フロント画面テスト1本が green になり、資料手順に足りない指示（追加依存・設定）が特定できている。
- ゴール: flow.html と slides.html から「レート制限」機能と「Workers KV」への言及を完全削除（代替注記は残さない）。関連 SVG も整合。
- 前提: KV の唯一用途はレート制限のため機能ごと削除。完了判定（POST /api/contact・GET /api/me）はレート制限非依存で影響なし。flow.html 1054行 CodeReviewSample.png（/code-review デモ alt の「レート制限なし」）は別文脈スクショで対象外。
- ステップ:
  1. flow.html 編集（10箇所）— 完了条件: KV/レート制限/429/「一時カウンタ」記述が消え D1 単独に整う
  2. img/tech_stack.svg 編集（D1/KV ボックス→D1 単独）— 完了条件: 図中に KV 表記が残らない
  3. slides.html 編集（14箇所）— 完了条件: flow と同内容で KV/レート制限が消え d1_vs_kv.svg 参照を除去
  4. 全体 grep で残存ゼロ確認（1054行除く）
- 保留: img/d1_vs_kv.svg は参照除去後に未使用化。ファイル削除は破壊的操作のため実施せず報告で判断を仰ぐ。

---

## 別タスク: slides-v2.html（「日の冒頭にまとめて説明」版の試作）✅ 作成完了（ブラウザ目視のみ未）
- 方針追加（依頼者の指示）: Day1/Day2 の冒頭は全員同期。Day3 は Day2 が終わり次第、各自随時着手（Day3 当日のガイダンスは行わない）。
  → これに伴い「Day3 課題の案内」を Day2 ブリーフィング末尾に前倒し。Day1 で早く終わった人は Day3 に入れない（全課題がバック＋デプロイ前提）ため、画面追加・SCREEN_SPEC 拡充を受け皿にした。
- 背景: 現行は「説明 → 作業 → 説明 → 作業」の交互構成のため、講師が次の説明に進むタイミングを判断できない。長丁場・先行者を Day3 に進ませたい・極端に遅い人で時刻が崩れる、の3制約から「同期は日の冒頭のみ」に寄せる案を試作する。
- ゴール: slides.html は変更せず、slides-v2.html で構成の違いを目視確認できる状態にする。
- ステップ:
  1. slides.html を slides-v2.html にコピー — 完了条件: 差分ゼロのコピーができている
  2. Day1/Day2 のスライド順を並べ替え（content=説明を前半に集約、break=作業カードを後半に集約。相対順序は維持） — 完了条件: Day1/Day2 内で content が全て break より前にある
  3. 新規スライド追加（進め方1枚・BRIEFING divider×2・WORK divider×2） — 完了条件: 各日の説明ブロックと作業ブロックの境界がスライド上で明示される
  4. タイムテーブルスライドを新構成（ブリーフィング＋自由作業）に差し替え — 完了条件: 3日分の時間配分が新方式に一致
  5. ブラウザで開いて順序・枚数・描画崩れを確認 — 完了条件: JS エラーなし、Day1/Day2 の並びが意図どおり
- 非対象: Day3（元々オプション課題ごとに独立しており自由作業前提のため構成を変えない）、flow.html、facilitator-guide.md
