# [12] E2E テスト — 設計ドラフト

> `flow.html` の `[12]` セクションを書き換えるための下書き。2026-08-04 の検討結果。
> **第1部が参加者に配る文面**（そのまま flow.html に貼れる形）、**第2部が書き手向けメモ**（実測データ・設計判断・未決事項）。

---
---

# 第1部: 参加者に配る文面

## [12] E2E テスト（画面を通して確認する）★★☆

**お題**: 「これが起きたらお金を返せと言われる」事態を3〜5個決め、そのうち2〜3個をブラウザ操作のテストにする
**ツール**: Playwright（Codegen）＋ Claude Code
**前提**: [6] まで完了していること（フロント・バックが繋がって動く状態）／[8] Stripe・[9] Clerk は未実施であること
**学び**: 手動では毎回やらない確認（他人のデータが見えないこと・データが保存されていること）を、自動で確認できる形にできるようになります

### 全体の流れ（80分）

| 経過 | やること | 成果物 |
|---|---|---|
| 事前 | Playwright 導入 | `playwright.config.ts` |
| 0–15 | ① お金を返せと言われる事態を3〜5個書く | `backend/docs/TEST.md` に追記 |
| 15–30 | ② 表にして、書くものを2〜3個選ぶ | 同ファイルに表 |
| 30–50 | ③ ◎ の操作を録画する | `tests/*.spec.ts`（たたき台） |
| 50–68 | ④ 検証を足して実行し、結果を表に記録する | 緑／落ちたの判定が入った表 |
| 68–80 | ⑤ 壊して赤を確認・コミット | — |

①と②が人間の作業、③④は AI と Codegen に任せられる部分が多くなります。

---

### 事前準備（Day2 の終わりに済ませてください）

リポジトリのルートで実行します。

```bash
npm init playwright@latest
```

対話プロンプトでは **chromium だけを選んでください**。Enter 連打だと firefox と webkit もダウンロードされ、数分待つことになります。テストの実行時間も3倍になります。

生成された `playwright.config.ts` に1行足します。

```typescript
use: {
  baseURL: 'http://localhost:8787',   // wrangler dev のポート。5173 ではありません
},
```

> ⚠️ ブラウザのダウンロードに数分かかります。当日ではなく Day2 の終わりに済ませてください。

---

### ① お金を返せと言われる事態を書く（15分）

まずアプリを起動します。

```bash
cd backend && npm run build:front && npm run dev
```

`http://localhost:8787` を開いて、**購入 → マイページ → 問い合わせを一通り操作してください**。自分のサービスに月300円払った人の立場で触ります。

そのうえで `backend/docs/TEST.md`（[5] で作ったファイル）の末尾に追記します。

```markdown
## [12] お金を返せと言われる事態

- 他人のメールを指定すると他人のライセンスキーが見えてしまう
- 購入したのにライセンスキーが発行されない／保存されていない
- ライセンスキーが末尾4桁以外も表示される（漏洩）
- 問い合わせを送ったのに届いていない
```

書いたら各行について、**画面のどこを見れば OK / NG が分かるか**を確認してください。

| 書いたもの | 画面のどこを見るか | 判定 |
|---|---|---|
| エラーが出ない | **決まらない** | ✕ 書き直す |
| ちゃんと表示される | **決まらない** | ✕ 書き直す |
| 他人のライセンスキーが表示されない | マイページに A のキー文字列があるか | ⭕ |
| 購入後にキーが発行され保存される | `/success` にキーが出ているか・再読込しても残るか | ⭕ |

「エラーが出ない」が使えない理由は、画面のどこを見ても確認できないからです（何も起きていない状態と区別がつきません）。見る場所が言えた行は、そのまま ③ の「期待結果」列に書けます。

**操作中に疑問が浮かんだら、その場で同じファイルにメモしてください。**

```
- マイページのメール欄、他人のアドレス入れたらどうなる？
- ライセンスキー、全桁出てるけどこれでいいの？
- 同じメールで2回購入ボタン押したら？
```

**やってよいこと / やらないこと:**

- ⭕ 動いている画面を見る
- ⭕ 自分が書いた `SCREEN_SPEC.md` を見る
- ✕ `backend/` `frontend/` のコードを開く

> ⚠️ コードを開くと「今こうなっている」に引きずられます。AI が書いたバグをそのまま「守るべき項目」として固定してしまうので、①が終わるまでは開かないでください。

---

### ② 表にして書くものを選ぶ（15分）

①で書いた項目を表にします。Claude Code に頼めます。

```
backend/docs/TEST.md の「[12] お金を返せと言われる事態」に私が書いた項目を、
同じファイルに表として書き直して。

列は「保証項目 | E2Eで確認できるか | 前提条件 | 期待結果 | 書く | 結果」。
「E2Eで確認できるか」だけ埋めて、残りは空欄にして——そこは私が埋める。
実装コードは読まないこと。
```

こういう表になります。右の4列を自分で埋めます（`結果` 列は④の後）。

```
| 保証項目 | E2E | 前提条件 | 期待結果 | 書く | 結果 |
|---|---|---|---|---|---|
| 他人のライセンスが見えない | ⭕ | Aで購入済み・Bは未購入 | Bの画面にAのキーが出ない | ◎ | |
| 購入するとキーが発行され残る | ⭕ | 購入のない新しいメール | 事前「購入なし」→事後キーが再読込後も表示 | ◎ | |
| キーは末尾4桁のみ表示 | ⭕ | 購入済み | 全桁が画面に出ない | ◎ | |
| 問い合わせメールが届く | ✕ | — | — | 実送信は確認できないので落とす | |
```

**行ごとの処理:**

| 状態 | やること |
|---|---|
| `E2E` が ✕ | **落として理由を1行書く**（画面から確認できないもの: 実際のメール送信、内部のリトライ回数など） |
| `E2E` が ⭕ | `前提条件` と `期待結果` を書いて、◎ を付ける |

**◎ は2〜3個までにしてください。** ◎ 1個がテスト1本で、録画1回ぶんの時間（5〜7分）がかかります。

**`前提条件` 列は必ず埋めてください。** ここが空欄だと、④ で「前回のテストで作られたデータを見て緑になる」テストができてしまいます。

**◎ を付けなかった行にも理由を1行残してください。** 「なぜこの2〜3個だけでよいのか」を説明するとき、落とした理由がそのまま答えになります。

> この時点では「実装されているか」を調べません。**④でテストを実行すれば分かります。**

---

### ③ 操作を録画する（20分）

**◎ 1個につき1回、リポジトリのルートで実行します。**

```bash
cd <リポジトリのルート>
npx playwright codegen http://localhost:8787/ -o tests/01-other-user.spec.ts
```

> ⚠️ `-o` のパスは実行した場所からの相対パスです。`tests/` の中で実行すると `tests/tests/...` を書きに行ってエラーで落ち、**録画した内容が消えます**。必ずルートで実行してください。

「他人のライセンスが見えない」の場合、1回の録画で A と B の両方を操作します。

1. 料金 → 購入する → メール `a@example.com` → 購入 → キーが表示される
2. マイページ → メール `b@example.com` → 確認する
3. ブラウザを閉じる → ファイルが生成されます

**検証（目玉マーク）は入れなくてかまいません。** 「A のキーが**出ない**こと」は目玉マークでは指定できないので、④ で手で書きます。

**録画中の注意:**

| | 理由 |
|---|---|
| 実名・実メールを入れない | テストコードは Git に残ります。`テスト太郎` `test@example.com` を使ってください |
| Enter キーを使わない | フォームが意図せず送信され、余計な行が2〜3行増えます |
| 迷ってクリックしない | 押した操作すべてがコードになります |

---

### ④ 検証を足して実行する（18分）

録画された内容は**操作の列だけ**です。検証を足します。Claude Code に頼めます。

```
tests/01-other-user.spec.ts を backend/docs/TEST.md の表の行
「他人のライセンスが見えない」に合わせて仕上げて。

- メールアドレスは毎回ユニークにする（Date.now() を使う）
- A の購入後に、発行されたライセンスキーを変数に捕捉する
- B のマイページで、捕捉したキーが表示されないことを確認する（toHaveCount(0)）
- テスト名を「他人のライセンスが見えない」にする
- 表の「期待結果」列の値をそのまま使い、実装コードから推測しないこと
- 固定待ち（waitForTimeout）は入れないこと
```

仕上がりはこうなります。

```typescript
test('他人のライセンスが見えない', async ({ page }) => {
  const emailA = `a-${Date.now()}@example.com`;
  const emailB = `b-${Date.now()}@example.com`;

  // A で購入してキーを捕捉
  await page.goto('/');
  await page.getByRole('link', { name: '料金' }).click();
  // …購入操作（録画された行）…
  const keyA = (await page.getByText(/^GENMAI-/).textContent())!;

  // B でマイページを開く
  await page.goto('/mypage');
  await page.getByLabel('メールアドレス').fill(emailB);
  await page.getByRole('button', { name: '確認する' }).click();

  // A のキーが1つも出ない
  await expect(page.getByText(keyA)).toHaveCount(0);
});
```

**「保存されている」を確認する場合は3つ揃える必要があります。**

```typescript
const email = `e2e-${Date.now()}@example.com`;            // 1. 毎回違うメールを使う

await expect(page.getByText('購入なし')).toBeVisible();    // 2. 購入前は空だと確認

const keyA = (await page.getByText(/^GENMAI-/).textContent())!;   // 操作して値を捕捉

await page.reload();                                       // 3. 再読込しても残る
await expect(page.getByText(keyA)).toBeVisible();
```

3つのうち1つでも欠けると確認になりません。

- 再読込だけ → 前回のテストで作られたデータを見て緑になります
- 違うメールだけ → 「画面に出た」は「保存された」の証明になりません（API の返り値を表示しているだけの可能性があります）
- 購入前の確認だけ → 何が増えたのか分かりません

`page.reload()` はページ全体の再読み込みです。画面が持っていたデータが破棄され、サーバーへ取得しに行きます。その値は D1 から来るので、保存されていなければ表示されません。

実行します。

```bash
npx playwright test tests/01-other-user.spec.ts
npx playwright test --ui        # 落ちたらこれ。ステップごとの画面が見えます
```

| エラー | 原因 |
|---|---|
| `TimeoutError: locator...` | アプリが落ちている（8787 を確認）／指定した要素が違う |
| `expect` が失敗 | API が失敗している。`--ui` でネットワークを確認 |
| 画面の内容が古い | `npm run build:front` を忘れている（`wrangler dev` は `dist/` を配信します） |

#### 落ちたら原因を切り分けます

落ちる原因は2つあります。**テストの書き方が悪い**のか、**実装がその項目に対応していない**のか。

```
tests/01-other-user.spec.ts が落ちた。
実装がこの項目に対応していないのか、テストの書き方（ロケーター・期待値）が
悪いのか判定して。実装が未対応なら直さず、そう報告するだけにして。
```

判定に応じて表の `結果` 列を埋めます。

```
| 保証項目 | E2E | 前提条件 | 期待結果 | 書く | 結果 |
|---|---|---|---|---|---|
| 他人のライセンスが見えない | ⭕ | … | … | ◎ | **落ちた → 未対応（バグ）** |
| 購入するとキーが発行され残る | ⭕ | … | … | ◎ | 緑 → 保証できる |
| キーは末尾4桁のみ表示 | ⭕ | … | … | ◎ | **落ちた → 未実装** |
```

**落ちた項目の実装は直しません**（今日の時間には入りません）。「作ったつもりが作られていなかった」ことを見つけた記録として残します。この表が「何を保証できて、何ができていないか」の一覧になります。

---

### ⑤ 壊して赤にする・コミット（10分）

**緑になったテスト**が本当に何かを守っているか確認します（落ちたテストは対象外）。

```
このテストが効いているか確認したい。
backend の該当ハンドラを1箇所だけ一時的に壊して、テストが赤くなることを見せて。
確認できたら必ず元に戻して。
```

- **赤くなった** → そのテストは機能しています
- **緑のまま** → **そのテストは何も確認していません**。④ に戻って `expect` を書き直してください

`.gitignore` を確認します（`npm init playwright` が書いていない場合は追記）。

```
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
```

```
E2E テストの追加をコミットして push して。
```

---

### ✅ 完了の定義

- ① を自分の言葉で書き、各行について**画面のどこを見れば OK / NG が分かるか**を言える
- ② の表に ◎ が付き、**◎ を付けなかった行の理由を言える**
- ◎ の項目についてテストを実行し、**表の `結果` 列が埋まっている**（緑／未対応）
- **わざと壊すと赤くなることを、緑になったテスト1つ以上で確認した**
- `test-results/` `playwright-report/` をコミットしていない

> 全部が緑にならなくてかまいません。落ちた項目は「作ったつもりが作られていなかった」という発見で、それも成果です。

---

### ⚠️ ハマりどころ

1. **`frontend` の `npm run dev`（:5173）で録画してしまう** — API が動かないので送信も購入も失敗します。`backend` の `wrangler dev`（:8787）を使ってください
2. **`tests/` の中で `codegen -o tests/...` を実行する** — エラーで落ち、録画が消えます
3. **`expect` が弱い** — 「見出しが見える」だけだと壊しても赤になりません。⑤ で必ず露見します
4. **`npm run build:front` を忘れる** — フロントを変更したのに画面が変わらない場合はこれです
5. **同じメールアドレスを使い回す** — 2回目以降、前回のデータを見て緑になります

---
---

# 第2部: 書き手向けメモ

参加者には配らない。flow.html への反映時の判断材料。

## A. 実測データ（DEMO/testAi-run・2026-08-04）

### 環境

- `frontend` の `npm run dev`（:5173）では API が動かない。`backend/wrangler.jsonc` の `assets.directory` が `../frontend/dist` を指し、`run_worker_first: ["/api/*"]` で Worker が API を処理する構成のため、**画面と API が両方動くのは :8787 だけ**
- Playwright の導入は chromium がキャッシュ済みなら4秒で完了。firefox / webkit は未キャッシュなのでダウンロードが走る

### ロケーター品質

shadcn/ui + Radix でも Codegen はアクセシビリティベースのロケーターを出せた。実測した録画では全操作が `getByRole` で取れ、CSS セレクタは `page.locator('form')` の1箇所のみ。

AI が実装から導く場合も同じ結果（`Contact.tsx` の `<Label htmlFor="name">` から `getByLabel('お名前')` が導ける）。**Codegen をロケーター調査のために使う必要はない**——④で使うのは操作列を作るためだけ。

### 1回目の録画で実際に発生したミス

| ミス | 結果 |
|---|---|
| フローごとに Clear せず1回で全部録画 | 4フローが1本に混在（34行・検証ゼロ） |
| 目玉マークを使わない | `expect` が0件。画面が真っ白でも緑になる |
| 実名・実メールを入力 | 実名と実アドレスがテストコードに残った |
| `tests/` の中で `-o tests/...` | ENOENT で落ち、録画が失われた |
| ロゴを2回クリック・Enter キー | 無駄な行が5行以上増えた |

2回目は `-o` でファイル直接出力＋1フロー1回起動にしたところ、`expect` が正しく入った。**「1フロー = 1回の codegen 起動」が確実。**

### 料金プランが2つあるため `.first()` が付く

```typescript
await page.getByRole('button', { name: '購入する' }).first().click();
```

月額プラン / 年額プランの両方に同じボタンがあるため。プランの並び順が変わると別のボタンを押すので、⑤ でプラン名で絞る形に直させる。「録画の出力をそのまま使うと壊れやすい」実例として使える。

## B. 設計判断とその理由

| 判断 | 理由 |
|---|---|
| **逆Gherkin化・差分表の工程を置かない** | 「実装がどうなっているか」は**④でテストを実行すれば分かる**。AI に実装を読ませて列挙させるのは推測を挟む二度手間で、記事自身が「AI は内部仕様と観測可能な結果を混同する」と警告している。加えて逆Gherkin化はレガシー理解の手法であり、3日前に自分が作った4画面には不要 |
| **落ちたテストの実装を直させない** | 80分に実装修正は入らない。「作ったつもりが作られていなかった」の発見を成果として表の `結果` 列に残す。全部緑にすることを目標にしない |
| `.feature`（Gherkin）を使わない | Cucumber を使わない方式では `.feature` は実行されない = 生成の入力として1回読まれるだけの死んだファイルになる。テストコードと乖離しても誰も気づかない。表に「前提条件」列を足せば Given の役割は果たせる |
| Gherkin ガイドの標準プロンプト6要件は借用する | トレーサビリティの相手を `.feature` から表に変えるだけでそのまま使える（④のプロンプトに反映済み） |
| Codegen を ② の後に置く | 何を検証するか決める前に録画すると実装追認テストになる。順序がこの工程の全部 |
| Codegen を主工程に残す | 「月300円払った人として画面を触り、これは正しいと判断しながら記録する」なら仕様の記録として成立する。操作列を作るコストが下がる。ただし「〜が起きない」の検証は録画できないので手書き |
| 1機能に絞らない | ① で書く項目は機能横断的に出る。機能を1つ選ばせると一番痛い項目を捨てさせることになる。絞るのは機能ではなく保証項目（◎ 2〜3個） |
| ① で正常系を書かせない | 正常系は録画すれば手に入る。人間が書く価値があるのは AI が思いつかない失敗の方 |
| 「顧客」を「月300円払った人」と書く | 参加者に実在の顧客はいない。金額を出すと想像しやすくなる |
| 難易度を ★★★ → ★★☆ に下げる | 判断の対象を「お金を返せと言われる事態」に絞ったため |
| 「テストが1本動いた」を完了条件にしない | 判断がゼロでも満たせるため学習目標を測らない。録画するだけで到達してしまう |

## C. Clerk / Stripe を実施した参加者向け（[9] / [8]）

`clerk.signIn()` は UI 操作だけを飛ばし、**認証の実体は本物を通る**（Clerk API を叩いて本物のセッショントークンを発行する）。環境変数が違えば失敗し、バックエンドが検証できなければ 401 になる。

ただし `signIn()` では検証できないものがある（`<SignIn />` の配置、ログイン後のリダイレクト、未ログイン時の遷移）。これは自分のコードの責任なので、**1本だけフォームを操作するテストを残す**。

```bash
npm i -D @clerk/testing
```

```typescript
// 1本目: フォームを実際に操作（Clerk の組み込み検証）
test('ログインしてマイページに入れる', async ({ page }) => {
  await setupClerkTestingToken({ page });     // ボット検知の回避。無いと "Bot traffic detected"
  await page.goto('/mypage');
  await expect(page).toHaveURL(/sign-in/);    // 未ログインで弾かれる
  // …フォーム操作…
  await expect(page).toHaveURL('/mypage');
});

// 2本目以降: プログラムからログイン
test('本人の購入だけが表示される', async ({ page }) => {
  await page.goto('/');
  await clerk.signIn({ page, emailAddress: process.env.E2E_CLERK_USER_EMAIL! });
  await page.goto('/mypage');
});
```

Global Setup に `clerkSetup()` が必要。

```typescript
setup.describe.configure({ mode: 'serial' })
setup('global setup', async ({}) => { await clerkSetup() })
```

この設定で15分ほど増えるため、80分に収めるなら **[9] は [12] の後にやるよう案内する**のが現実的。

Stripe（[8]）を実施した場合、購入フローが Stripe Checkout（外部ホストの DOM）に飛ぶ。Checkout 画面は E2E で操作させない（先方の UI 変更で壊れる）。購入フローの E2E は Checkout の手前までにする。

## D. 未決事項

1. **[12] を Day3 のオプションに残すか、Day2 [5] に統合するか**
   E2E は「他の変更を守る安全網」なので、他のオプションと並列の選択肢にするのは構造的に噛み合わない。[7]〜[9] を追加する前に E2E があってこそ機能する。Day2 [5] の末尾に1〜2本置き、Day3 の各オプションに「追加したら `npx playwright test` を回す」の1行を入れる案が有力（ただし Day2 が伸びる）
2. **Clerk のログインフォームのロケーターが取れるか未検証** — `DEMO/conv-demo/conv-demo-2026-06-08` で実測が必要
3. **`+clerk_test` メール（固定コード `424242`）が使えるか未検証** — 使えればテストユーザーの管理コストがほぼ消える
4. **`gherkin-test-generator` スキルは使わない** — 要件定義書が正本にある前提のスキルで、参加者には正本がないため実装から生成される（= 実装追認）

> 逆Gherkin化（[Qiita 記事](https://qiita.com/tsuruta_y/items/562fccd7408e82d7b46b)）の採用は**見送りで決着**。理由は §B の1行目。

## E. 社内ガイドとの関係

社内ガイド索引 `docs/e2e-test-guide/README.md`（AI Review Toolkit `creationline/aidd-force-team-b`）は二者択一として定格化されている。

- 仕様を正としてコードへ落とす → Gherkin × AI
- 画面操作でたたき台を素早く取る → Playwright Codegen

**組み合わせる流れは定義されていない。** また両ガイドとも「何をテストすべきか」は扱っていない——そこが [12] のテーマ。

Codegen ガイドを参考資料として配る場合は1行添える。

> ⚠️ 録画は「今こう動いている」の記録であり、「こう動くべき」ではありません。録画したテストが緑になっても、今の実装が今の実装どおりであることしか意味しません。
