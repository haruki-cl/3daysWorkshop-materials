# flow.html レビュー ピックアップ

## A. 誤り・矛盾（直さないと参加者が詰まる）

* そもそも何を言っているかわからない。（）で情報を付加しているつもりなのでしょうが、同じ行と判断されかねないかと。あとそもそも確かめる必要のない項目だったはテストの書き方が悪いのではなく、「不必要な設計が行われていた」では？そもそもプロンプトにおいてもこれらは判別させていないため、急に表に現れた未知の何か
* あと「⑥ テスト結果 で処理済み（④ テスト項目作成 からやり直す）」これは日本語的に本当に意味がわからない。

**| ⑥ の判定                                                              | 直す対象                                                   |
| ------------------------------------------------------------------------ | ------------------------------------------------------------ |
| 実装が要件を満たしていない                                             | 実装を直させる（テストファイルは変更禁止）                 |
| テストの書き方が悪い（前提条件を作っていない、要素の指定が違う、など） | テストを直させる（実装は変更禁止）                         |
| テストの書き方が悪い（そもそも確かめる必要のない項目だった）           | 直さない。結果列に判定と理由を書いて閉じる                 |
| 要件そのものが間違っている                                             | ⑥ テスト結果 で処理済み（④ テスト項目作成 からやり直す） |

**


## B. 分かりにくい・誤読を招く

* 「購入したキーが消えない」なら、破られたときに「金を受け取ってモノを渡していない状態になる」と言える。これは載せる。落ちるのは次のような候補である。」言えるため、これは載せる。と一文で
* 挙げるのは、自分が Section 1 と 2 で作った画面、API、DB の3層のどこかである。」「通し」を含めた4項目で見る必要があるため、そもそもこの行はミスリード。消しておくくらいがいいかも
*

## C. 表記・体裁・掃除

## D. HTML / JS の実装

## E. 改行判断

## F. 削除要請(Cでの削除対応が増えたため)

* 載せると決めた項目が、そのまま並んでいる。→冗長。消す
* テスト設計を行うと、このような表となる（玄米茶サンプル。自分のアプリでは中身が変わる。ここでは要件2つ分だけ抜粋しており、実際は ① テスト要件 で書いた要件すべてについて行が続く）。」テスト設計表はトグルで閉じられているため、この行を表で書くと資料全体が重くなる。自明のことが多いため、まとめて消してもいいかも
* 要件を壊れる場所ごとに分けて行にしてあるので、落ちた行がそのまま原因の場所を指す。T-001 が落ちたなら画面、T-003 が落ちたなら保存処理である。」トグル引用をやめてくれ。消して

## G. 追記要請

## H. 検証必要性あり

* ここから実装のセッションに戻る。この ③ テスト環境準備 以降は実装コードを読ませてよい。」戻る必要はあるのか？
* **出力の skipped が0件であることを、人間が自分で見る。** skipped があってもテスト全体は成功として扱われるので、画面は緑のまま、確かめた本数だけが足りなくなる。」そもそもテスト項目作成段階でskipさせないようにする旨をプロンプトにぶち込めばいいのではと言う検証が必要そう

## 確認してほしい判断（今回こちらで決めたもの）

### [6] 機能テスト（今回分）

* **⑥ の突き合わせプロンプトに「通しの行を突き合わせから外す」を戻した。** 前ラウンドで「無駄」として消した指示なので、判断が反転している。あわせて下の箇条書きから「（「通し」の行はここに出るのが正しい）」を削除した
* **`git diff` の `--stat` を外した。** 指摘のとおり、1ファイルに複数テストがある場合はファイル単位の増減しか出ず、⑦ で要求している「期待結果の値が変わっていないか」を見られない。⑦ 手順3 の末尾も「`git diff` で中身も見る」→「変更の中身まで見る」に整理（コマンドが1つになったため）
* **「要件を数え上げきる」は「テスト要件で守るべきことを漏れなく列挙しきる」に。** 提案の「全ての要件をテスト化することは目標にしない」は要件→テスト項目の網羅性の話で、①（要件の列挙）とは別の主張になる。しかも [6] は「結果列を埋めきる」を完了条件にしているので、そう書くと矛盾する

### [6] ⑦ コード修正（前回分）

* **⑥ の3判定のうち「テストの書き方が悪い」に処理が無かったので追加した。** 手順1 を判定ごとの表にし、「テストを直させる（実装は変更禁止）」の行を足した。あわせて次を調整
  * テストを直させるプロンプトに「期待結果は `docs/TEST.md` の表の文言から変えないこと」を入れた。これが無いとテスト修正の許可がそのまま Reward Hacking の入口になる
  * 手順3 の git 差分チェックを「差分が出たら NG」→「**出た場所が自分で指示したテストだけか**を見る。テストを直させた行は中身も見て、期待結果の値が変わっていたら差し戻す」に変更
  * ポイントの「テストの書き方が悪い行は緑にしようがない」を「**そもそも確かめる必要のない項目だった**行」に限定
  * これで [13] E2E ④ の判定表（「テストの書き方が悪い → AI にテストを直させる」）と扱いが揃った
* **⑦ 末尾の重複コミットを削除した。** ⑧「壊して赤」を消したときにコミットだけ残してしまい、直後の「完了したら push する」と同じ作業が2回並んでいた

### Section 3（前回分）

* **ポートの固定表記を直した。** 指摘のとおり `wrangler dev` のポートは環境で変わるので、[13] ⓪ 準備 に「`npm run dev` の出力に表示されたものに合わせる（既定は 8787 だが、使用中なら別のポートが割り当てられる）。以降このページに出てくる 8787 も自分の環境のポートに読み替えること」と1回だけ書いた。**本文中の `8787` 自体は書き換えていない** — codegen のコマンド例など具体値がないと形が示せない箇所が多く、全部を `<ポート>` に置き換えると読みにくくなるため
* **[9] 決済のファイル指定を全部落とした。** 見出しの「実装ファイル（ここに作る）」、② の `stripe.ts` / `license.ts`、③ の前提文の3箇所。③ の前提は「② の実装が完了してから進む」に置き換えた
* **[10] Clerk の `GET /api/me` を「API」に置き換えた。** Section 2 でパス固定の記述を消したので、ここだけ残っていた
* **SonarCloud の重複**は、外側の「誤って All repositories を選んでしまった場合は、次のどちらかで対処する」を残し、内側の「すでに「All repositories」で許可してしまった場合は、あとから絞れる」を消して入れ子を1段浅くした

### Section 1 まで

* **「Section 2 における環境確認タイム」→ Section 1 のものと解釈した。** 前準備の直後（`ps-overview`）へ「環境確認」として移し、**Section 1 はセキュリティ対策から始まる**形にした
* **料金プランの記述**は「LP に料金プランを載せる。プラン数や金額は自由だが、サブスクリプション（月額／年額など）であることは必須」に書き換え、見出しも「推奨に従うもの」→「要件として外せないもの」に変更
* **玄米茶サンプルの説明文**は、サンプル画像5枚の導入文が消えるため「資料中の例「玄米茶」は説明用の共通サンプルである。」の一文だけ残した
* **Section 1 クロージングの削除**は節のみ。タイムテーブルの「1:50–2:00 クロージング」の行は時間枠として残した

### Section 2（番号ずらし）

* **[5] レビュー／[6] 自動テスト に分け、以降を [7]〜[14] へ繰り上げた。** `id`・ゴール表・タイムテーブル・目次・Section 3 の相互参照・範囲表記まで追従済み。`img/workshop_flow_diagram.svg` は Core 行を 6box → 7box に組み直した
* **「壊して赤になるか確認する」は [6] だけでなく [13] E2E テストの ⑤ も削除した。** **E2E 側は残す意図だった場合は戻す**

### 文体・体裁（今回分）

* **「だ・である調」への統一を全体に通した。** ps-overview / Section 1 / Section 2 / **Section 3 / 付録** まで適用済み
  * です・ます調は Section 3・付録で4箇所あり、すべて直した（発表会の案内文、Resend の API キー方針、Clerk のログイン方式、付録の見出し）
  * 体言止めは**本文（`<p>` と説明文の `<li>`）だけ**を「〜である」に直した。**表のセル・見出し・`summary`・手順ラベル（「お題:」「学び:」「ツール:」等）・箇条書きの列挙は体言止めのまま**にしてある。全部直すと冗長になるため
  * 残っている「です・ます」は2箇所あるが、いずれも**プロンプト例の中の文字列**（「このセクションは不要です」）と**画像の alt テキスト**なので、資料の文体ではないと判断して触っていない
* **[8][9][10] 共通の見出し**を「[8][9][10] 共通：シークレット管理」に変更した（目次も追従）。節の中を指す参照文（「共通の手順参照」「共通の秘密情報の置き場所参照」）はリンク先が変わらないのでそのままにしてある
* **⑦ コード修正 の結果列の表も `<details>` に畳んだ**（「結果列を埋めた表の例を見る」）。導入文は「結果列を埋めると、次のような表となる」に変更

### Section 2（前回分）

* **⑥ テスト結果 の突き合わせプロンプトを簡素化した。** 「grep で抽出して diff で」と「通しの行を外して別に列挙して」の2点を削除し、番号の突き合わせを頼むだけにした。あわせて下の箇条書きの「外した番号 →」の行も落とし、「表にあってコードにない」の説明に「通しの行はここに出るのが正しい」を吸収させた
* **⑦ コード修正 のポイントを「結果列を埋めきること」に書き換えた。** 「テストの書き方が悪い」と判定された行は緑にできないので、判定と理由を結果列に書いて閉じる、という形にした。完了の定義も「「通し」以外の行が全部緑」→「緑になっているか、緑にしない／できない理由が結果列に書かれている」に変更
* **丸番号の参照を「丸番号 セクション名」に統一したのは [6] 自動テストの中だけ。** [13] E2E テストの ①〜⑤ は工程名を付けていないので手を付けていない。必要なら次のラウンドで
* **③ テスト環境準備 の「「関数」の行しかなければ…」の段落は丸ごと削除した。** 代わりに「やること」に「（通しについては今は除外）」を入れてある

## 検証ログ

### R2（見た目レビュー）

* ✅ 全5ページ（概要 / Section 1 / Section 2 / Section 3 / 付録）をヘッドレス Chrome で幅 1440px・実寸レンダリングし、全高をタイル分割して目視した
* ✅ レイアウト崩れ（要素の重なり・はみ出し・横スクロール）は検出なし。テーマ配色・toggle・コピーボタン・目次ハイライトも見た目上問題なし
* ⚠️ 撮影時、高さ 5 万 px 超のキャンバスで Chrome ヘッドレスの描画が壊れて白帯が出た（[14] 付近）。短縮ページで撮り直して実レイアウトは正常と確認済み。資料側の問題ではない

### 過去分

* ✅ アンカー切れ: 0 件
* ✅ 画像の参照切れ: 0 件
* ✅ ラベルとアンカーの整合: 不一致 0 件
* ✅ 文体スキャン（全体）: 資料本文の です・ます 0 件
* ✅ ブラウザ確認: 見出し・工程名・toggle が意図どおり。コンソールエラーなし（スクリーンショットは環境側の不具合で撮れず、DOM で確認した）
* ⬜ フロー図の**見た目**の確認: Core 行を組み直したので、7 box が枠内に収まっているか目視してほしい
* ⬜ 外部リンクの生存（デモ URL・各サービスのログイン URL）: 未確認

---

# R11: Section 2 レビュー（[4]〜[7]）

Tier 1・Tier 2 の48件は flow.html に適用済みのため削除した。以下は未対応分。

## 見送り（ワークショップの流れ・作業が変わるため今回は直さない）

- ⬜ **R11-02** `flow.html` [4] 手順3 — モック認証を3回言及しているのに、実装させるプロンプトがどこにもない。ポイントの参照先（手順3）にも記述がない。※ R11-58 で実装形（`x-user-email`）の説明だけ足した。プロンプト側は未着手
- ⬜ **R11-03** `flow.html` [6] ⑤ テスト結果 — 「テストを実行させ」と宣言しているが、実行する手順がない。手順2 は「赤が出たときだけ」と言うが、実行しないと赤か分からない
- ⬜ **R11-05** `flow.html` [4] npm scripts — `db:migrate:local` / `build:front` を、script を定義する節より前の手順で叩かせている
- ⬜ **R11-06** `flow.html` npm scripts 表 `test: vitest run` — vitest の導入は [6] ③ なので、[4] の時点では動かない
- ⬜ **R11-08** `flow.html` npm scripts — `wrangler d1 migrations apply DB` の binding 名 `DB` が、AI の生成物と一致する保証がない
- ⬜ **R11-11** `flow.html` [6] ② — 「関数 / API / 画面 / 通し」の意味を説明する表が、その語を使うプロンプトより後にある
- ⬜ **R11-55** `flow.html` [6] ② — ポイントの「テスト範囲が狭いほうを選ぶ」が、② のプロンプトに入っていない
- ⬜ **R11-56** `flow.html` [6] ④ — ④ だけ完了の判定がない

## H. 検証必要性あり

- ⬜ **R11-59** `flow.html` [6] ③ 「ここから実装のセッションに戻る」— ① ② を新セッションで始めた後、元のセッションに戻る手順（`claude --resume` 等）が書かれていない。そもそも戻る必要があるのかも要検証。※前ラウンドの H にも同じ指摘あり
- ⬜ **R11-60** `flow.html` [6] ④ / ⑥ 手順5 — ④ で `test.skip / it.todo / it.only` を禁止済みなのに、⑥ で skipped の検出に4段落を割いている。禁止が効くなら縮められるはず
- ⬜ **R11-61** `flow.html` [6] ③ プロンプト — 「設定は cloudflareTest プラグイン方式で書く」が実在の API 名か、動作確認時点（2026-07）から変わっていないか。`jsdom` / `testing-library/react` も未定義のまま出ている
- ⬜ **R11-62** `flow.html` [6] ① ポイント — 「モデルは高精度なもの（Fable / Opus）」。モデル名は変わりやすい。[1] のモデル設定の記述と重複していないか
- ⬜ **R11-63** `flow.html` [5] — 「既定の Medium で実行するのを推奨（Team standard など…）」。プラン名と Effort の既定値は変わりやすい。[1] の Effort 説明との重複も要確認
- ⬜ **R11-64** `flow.html` [5] — `/code-review` と `/security-review` を「Claude Code の組み込みコマンド」と書いている。現行バージョンで組み込みかプラグイン提供かを要確認
- ⬜ **R11-65** `flow.html` [6] ⑥ — 結果列の例で「通し」の行に「[12] で確認予定」と書かせているが、[12] は Section 3 の**オプション課題**。やらない参加者の結果列がどうなるかが決まっていない
- ⬜ **R11-66** `flow.html` [6] ⑥ — 手順1 で「私が言うまで追加のコミットはしないこと」と縛っているが、AI が守らなかった場合に手順4 の `git diff` が空振りする

## 確認してほしい判断（R11）

- **誤指摘だったので直していない2件。**
  - R11-25「`<ul>` の中身が `<li>` 1つだけ」— [4] 手順2（D1 の説明）は `<li>` が2つあり、指摘が誤りだった。手順6 の1件は R11-51 の削除で解消した
  - R11-30「details の summary の書式が不揃い」— 実際は「テスト項目の表の例を見る」「結果列を埋めた表の例を見る」で揃っており、指摘が誤りだった
- **用語統一（R11-19）の範囲。** [6] の「ライセンスキー」4件に加えて、例の表と本文の「キー」も「購入の証」に揃えた（「発行した購入の証が消えない」「購入の証を本人以外に返さない」など、計15件）。API キーの意味で使っている 995行の「キー」は残している
- 「通し」の行の重複6回と、前ラウンドの A / B / F 節の消し忘れ項目は、指示により手をつけていない

---

# R12: Section 3 レビュー（[8]〜[14] / ラップアップ・後片付け）

対象は flow.html 1439〜2512行。機械チェック（アンカー・画像・ラベル整合・タグ対応）は全て `none`。
各項目末尾の【】は影響度。【1】文言のみ・流れは変わらない／【2】要判断・軽い影響／【3】流れや作業が変わる／【H】要検証。

## A. 誤り・矛盾（直さないと参加者が詰まる）

- ⬜ **R12-01** `flow.html:2239-2243` [12] ④ — 「直させるときは『テストファイルは変更しないこと』を添える」と書いた直後の表が「テストの書き方が悪い → AI にテストを直させる」。テストを直させるのにテストファイル変更禁止では実行不能。[6] ⑥ は実装用とテスト用でプロンプトを出し分けている（1317行 / 1321行）のに、[12] は1文に丸めて誤りになっている。【2】
  - → 対応: [6] ⑥ と同じく判定ごとに添える文言を分ける
- ⬜ **R12-02** `flow.html:382,407,444,449` 範囲表記 — 「Section 3 の **[8]〜[13]** は任意」が4箇所にあるが、Section 3 には [14] 運用保守の自動化がある。しかも 1456行は「1つだけやるなら、軽い **[8]・[14]** から始めるとよい」と [14] を推奨している。[14] を後から足したときの追従漏れ。【1】
  - → 対応: 4箇所を [8]〜[14] に直す
- ⬜ **R12-03** `flow.html:2368` [13] ハマりどころ — 「必ず上記**手順3**の Build command を設定する」。Build command は手順4（2349行）、手順3 は Path の設定。参照番号がずれている。【1】
- ⬜ **R12-04** `flow.html:1922,1924,1935` [11] — 本文は「詳細画面の**5箇所（①〜⑤）**に印を付けた」、図の alt は「**6箇所**を赤枠と番号で示した」、その2段下は「上図**⑥**の『Why is this an issue?』タブ」。5 と 6 が混在している。【1】
  - → 対応: 本文を「6箇所（①〜⑥）」にし、プロンプトのひな形は①〜⑤のまま、⑥は補足として扱うと整合する
- ⬜ **R12-05** `flow.html:2291` [12] 完了の定義の note — 「[6] は『通し』以外の行を**全部緑にする**ところまでを完了条件にしている」。[6] の実際の完了の定義（1388行）は「緑になっているか、**緑にしない／できない理由が結果列に書かれている**」。[6] の要約が誤っている。【1】
- ⬜ **R12-06** `flow.html:2003 vs 2206` [12] — 「自分で考えるのは **① の行の選択と ④ の赤の判断**」と書いているが、③ にも「**自分でやること**: AI が書いたテストを開き、確認として足りているかを見る」（2206行）がある。工程の数が合わない。【1】
- ⬜ **R12-07** `flow.html:1700-1701 と 1539` [9] ⑤ / [8] — [9] は「モック認証の email に固定値（例: `test@example.com`）をセットし、Stripe Checkout の `customer_email` にも同じ値を渡せば一致する」と言うが、[8] の Resend は「**宛先は Resend に登録したアカウントのメールアドレス1つだけ**」に限られる。[8] と [9] を両方やると、購入の証の送付メールが `test@example.com` 宛になってエラーになる。課題どうしの衝突が書かれていない。【2】
- ⬜ **R12-08** `flow.html:2416-2418 と 1033` [14] — [14] は `/code-review` を「**組み込みスキル**」と呼び、[5] は同じものを「Claude Code の**組み込みコマンド**」と呼んでいる。スキルとコマンドの用語が混在している。【1】
- ⬜ **R12-09** `flow.html:2134-2135,2141` [12] ② — ファイル名の例が「T-008 キーを本人以外に返さない」「**T-010** 購入したキーが消えない」。T-010 は [6] の例の表（T-002〜T-009）に存在しない番号。さらに直後の説明は「番号は表の行番号（**T-005** など）に合わせる」で、例と説明で別の番号を出している。【1】
- ⬜ **R12-10** `flow.html:1980` [11] 完了の定義 — プロンプトが「セキュリティ修正の作業をコミットして **main ブランチに** push して」。他の課題（[8][9][10][12][13][14]）は全て `dev`。[11] だけ main への直接 push になっている。SonarCloud が main を解析するため意図的とも読めるが、[7] の「dev → main はマージで」という運用と衝突する。【2】

## B. 分かりにくい・誤読を招く

- ⬜ **R12-11** `flow.html:2231-2237 vs 1277-1284` [12] ④ / [6] ⑤ — 判定の選択肢が [6] は**4つ**（実装／テストの書き方／テスト設計が誤り／要件が誤り）、[12] は**3つ**（実装／テストの書き方／どちらとも言えない）。2230行で「理由は [6] ⑤ と同じである」と参照させているので、読み手は同じ4択だと思って読む。【2】
  - → 対応: [12] 側に「[6] とは選択肢が違う（E2E では仕様の未決が出るため）」の一行を足す
- ⬜ **R12-12** `flow.html:1987` [12] 前提 — 「**前提**: [7] まで完了していること（[6] の「## テスト項目」の表があること）」。括弧の中身は [7] ではなく [6] の条件で、括弧が直前の [7] を補足しているように読める。【1】
  - → 対応: 「[7] まで完了していること／[6] の『## テスト項目』の表があること」と並列にする
- ⬜ **R12-13** `flow.html:1789 と 1757` [10] 完了の定義 — 「`backend/.dev.vars` に記入済みの**シークレットのみ**を `wrangler secret put` で本番側に投入する」。だが 1757行の表では Publishable key（公開してよい鍵）も `backend/.dev.vars` に置かせている。「記入済みのシークレットのみ」だと Publishable key も対象に読める。【2】
- ⬜ **R12-14** `flow.html:2013 と 2088` [12] — `npm i -D @clerk/testing` を実行する場所が書かれていない。「リポジトリのルートで」と明示しているのは ⓪ 準備（2088行）だが、この折りたたみは ⓪ の**前**に置かれているので、読み手はまだそれを読んでいない。【2】
- ⬜ **R12-15** `flow.html:1443 と 1457` Section 3 冒頭 — 「★☆☆＝易しい / ★★☆＝ふつう / ★★★＝難しい」と定義した直後に「**★の数がそのまま重さの目安**になっている。★☆☆ でおよそ 30〜45分…」。同じ★に難易度と所要時間の2つの意味を持たせている。【1】
- ⬜ **R12-16** `flow.html:1460 と 2007-2086` [12] — 「[12] は **[9][10] より先にやる**」と言いながら、[12] の中で最大の分量（80行）が「[9][10] を実施済みの人向け」の折りたたみになっている。推奨と分量の重心が逆を向いている。【2】
- ⬜ **R12-17** `flow.html:2347-2373` [13] — 完了の定義は「上記**4つ**の設定を終えたら」だが、連携手順は 1〜5 の**5項目**。どの4つを指すのかが読み取れない。【1】
- ⬜ **R12-18** `flow.html:1556, 1715` [8] / [9] 完了の定義 — 「自分宛にメールが届けば**完了である**。ローカルで動作確認できたら、`wrangler secret put` で…`npm run deploy` までを**完了とする**」。1つの段落に完了条件が2つあり、どちらが完了か決まらない。[9] も同じ構造。【1】
  - → 対応: 「ローカルで届く」を必須、「本番投入まで」を到達点、と段階を分けて書く
- ⬜ **R12-19** `flow.html:2057` [12] — `set -a; . ./backend/.dev.vars; set +a` でシェルから `.dev.vars` を読ませている。一方 2410行は「`.dev.vars` は Section 1 の設定で **Read を拒否**している」と書いている。人間が打つコマンドなので実際は矛盾しないが、資料内で読み手が引っかかる。【2】
  - → 対応: 「これは自分のシェルで読むので、Claude Code の deny 設定とは別である」の一行を足す
- ⬜ **R12-20** `flow.html:2064,2099,2124,2278` [12] — ポートについて 2099行が「既定は 8787 だが、使用中なら別のポートが割り当てられる。以降このページに出てくる 8787 も読み替えること」と断っているのに、2064・2124・2278 で `8787` を決め打ちで再掲している。とくに 2278 のハマりどころは「`backend` の `wrangler dev`（**:8787**）を使うこと」と、読み替えの断りより後にある。【1】
- ⬜ **R12-21** `flow.html:1958` [11] — 「[11] の完了条件（1つ以上修正）は、**Reliability・Maintainability の指摘から選んでも構わない**」。だが章名は「セキュリティレビュー」、お題（1801行）は「セキュリティ観点で点検し、最低1つ直す」。保守性の指摘を直して完了、はお題とずれる。【2】
- ⬜ **R12-22** `flow.html:2406,2436` [14] — `.gitignore` に `.cf-ops.token` を追加、とあるが、ルートの `.gitignore` か `backend/.gitignore` かが書かれていない。ファイル自体は `backend/.cf-ops.token`。【1】
- ⬜ **R12-23** `flow.html:2454 と 2449` [14] — プロンプトは「スキルを**5つ**作って」＋統合スキル、完了の定義は「5つのうち**最低1つ**が動作する」。5つ作らせて1つ動けばよい、という設計の意図が読み取りにくい。【1】

## C. 表記・体裁・掃除

- ⬜ **R12-24** Section 3 全体 — 用語が「キー」34件・「購入の証」11件・「ライセンス」2件で混在している。Section 2 は R11 で「購入の証」に統一済みなので、**セクションをまたいで不整合**になっている。とくに 2134-2135（ファイル名の例の表）、2212-2217（`ライセンスキーの欄`／`GMC-••••-••••-FS9J`）、2302（`licenseMasked` / `license_masked`）。【1】
  - → 対応: Section 2 と同じ基準で揃える。ただしコード例の識別子（`licenseMasked` 等）は実装の名前なので、そのままにするか判断が要る
- ⬜ **R12-25** `flow.html:1848,1870` [11] の図の alt — `genmaichaLP`（リポジトリ名）と `kamei-cl`（組織名）が入っている。過去に「固有名 genmaicha_front/back を汎用プレースホルダに置換」をやっているのに、alt テキストが追従していない。社内資料としては固有名の混入。【1】
- ⬜ **R12-26** `flow.html:1802 と 1815` [11] — 冒頭の「ツール」は `/security-review` と `security-guidance` の2つだが、本文（1815行）は `/code-review` も挙げている。【1】
- ⬜ **R12-27** `flow.html:2102,2118,2187,2226,2262,2274` [12] — 各工程の末尾に「**完了**: …」の note を置く形式が [12] だけにあり、[8]〜[11][13][14] には無い。構成が不揃い。【2】
  - → 対応: この形式は分かりやすいので他章にも広げるか、[12] 内に留める判断をする
- ⬜ **R12-28** `flow.html:2500-2506` 後片付けの表 — 1つのセルに7行の手順（GitHub App の Uninstall と Revoke の2段階）が詰まっている。表のセルが手順書になっている。【1】
  - → 対応: 表からは「2箇所の解除が要る」だけを出し、手順は表の下か `<details>` に出す
- ⬜ **R12-29** `flow.html:2093` [12] ⓪ — 「Chrome 以外に Firefox と **Safari のエンジン**もダウンロードされる」。Playwright が入れるのは WebKit。「Safari のエンジン」は説明としては通るが、コマンドの出力に出る語と一致しない。【1】
- ⬜ **R12-30** Section 3 全体 — `p class="note"` が21件。[12] に集中している。注記が多すぎると本文との濃淡が消える。【2】

## D. HTML / 図

- ⬜ **R12-31** `flow.html:1826-1899` [11] — SonarCloud のアカウント作成手順に図が12枚並び、`<ol>` の1項目に1枚ずつ入っている。この章だけ画像の密度が突出しており、印刷・スクロールの負担が大きい。【2】
  - → 対応: アカウント作成〜組織作成（手順1〜8）を `<details class="spec">` に畳み、解析結果の見方から本文にする
- ⬜ **R12-32** `flow.html:2296-2300` [12] の details 内 — 経路図が `<pre><code>` のアスキーアートで描かれている。同じ資料の他の図は SVG。【2】

## E. 文のつながり

- ⬜ **R12-33** `flow.html:2230` [12] ④ — 「まとめて頼むと、AI はテストの期待値を緩めて緑にする道を**選べる**」。R11-37 で [6] 側を「選ぶ」に直したのに、こちらが残っている。【1】
- ⬜ **R12-34** `flow.html:2347-2348` [13] 手順3 — 「Path（Root directory）を `backend` に設定する（モノレポ構成のため。空のままにすると Cloudflare が自動生成した誤った設定でデプロイされてしまうので注意すること。詳細は下記ハマりどころ）」。括弧の中に理由・警告・参照の3つが入り、係り受けが切れている。【1】
- ⬜ **R12-35** `flow.html:2411` [14] — 「そこで運用トークンだけは deny 対象外の別ファイルに**分離する**。ここに入れるのは Analytics 読み取り専用の低権限トークンなので、高価値シークレットとは**分けて扱ってよい**」。同じことを2回言っている。【1】
- ⬜ **R12-36** `flow.html:1745-1747` [10] — 「Application name（**後でいつでも変更可**）」と「なおこの設定はダッシュボードから**後でいつでも変更できる**ので、作成後に直しても構わない」が同じ項目内で2回。【1】
- ⬜ **R12-37** `flow.html:2115` [12] ① — 「[10]（Clerk）の分は…で回避できる。[9]（Stripe）の購入操作そのものは回避できないので、前提条件を『購入済みのアカウント』に置き、購入後の表示を見る行に振り替える。振り替えられない行は選ばず、結果列に『…』と書く。決済画面の中の不具合は Stripe 側の設定に由来するので、直す先が自分のリポジトリにない。」— 1段落に条件・回避・振替・例外・理由の5つが連なっている。【1】
- ⬜ **R12-38** `flow.html:1969` [11] — 「プライベートリポジトリが含まれる場合はコードが外部に送られるため**注意したい**」。「注意したい」は主体が曖昧で、指示なのか感想なのか読めない。【1】
  - → 対応: 「〜ため、All repositories は選ばない」と行動で書く
- ⬜ **R12-39** `flow.html:1595` [9] ① — 「サンドボックス（テストモード）はデフォルトで有効（ワークショップは終始これ）。テストモード＝本物の課金が発生しない Stripe の練習環境のことで、[6] の『機能テスト』とは別物である」。3つの独立した内容が1項目に詰まっている。【1】

## F. 削除要請

- ⬜ **R12-40** `flow.html:1518 と 1540-1543` [8] — 「権限は Sending access で十分／Sending domain は All domains を選択」を手順2 の括弧で書いた後、blockquote で「本ワークショップでは "All domains" を選んで作成すること」と再掲している。【1】
  - → 対応: blockquote を削除し、理由（独自ドメインを検証しないと発行できない）だけ手順2 の括弧に足す
- ⬜ **R12-41** `flow.html:1846 と 1964-1969` [11] — 「All repositories を選ぶとプライベートリポジトリ全部が見える」を手順5 の warn で書き、ハマりどころで同じ内容を長く再掲している。【1】
  - → 対応: 手順5 は警告と「詳細は下記」だけにし、内容はハマりどころ側に一本化する
- ⬜ **R12-42** `flow.html:2075 と 2115` [12] — 「[9] 実施済みなら購入済みアカウントを用意し、購入後の表示を見る行に振り替える」が折りたたみ内と ① で2回。【1】
- ⬜ **R12-43** `flow.html:1888-1895 と 1906` [11] — Summary 画面の件数（Reliability 16 Open issues など）を箇条書きで出した直後、「『Summary』画面の数字（例: Reliability 16 Open issues）はあくまで件数の集計である」と同じ数字で再掲している。【1】
- ⬜ **R12-44** `flow.html:2226 と 2287` [12] — 「2本とも実行し、結果列が書き換わっている」が ③ の完了 note と完了の定義で2回。同様に 2108 と 2289 の「選ばなかった行は『[12] で確認予定』のまま」も2回。【1】
- ⬜ **R12-45** `flow.html:1958` [11] — 「Security の指摘が 0 件でも異常ではない」は、1890行で既に「Security: 0 Open issues／評価 A」を例として出しているので、驚きを打ち消すためだけの補足になっている。【2】

## G. 追記要請

- ⬜ **R12-46** `flow.html:1547 と 1539` [8] — 「宛先は Resend に登録したアカウントのメールアドレス1つだけ」という制約が、実装依頼「From / To をどうするかも含めて AI に相談して実装する」に反映されていない。AI は任意の宛先を実装しうる。【2】
  - → 対応: 依頼文に「宛先は Resend に登録したアドレス固定でよい」を足す
- ⬜ **R12-47** `flow.html:2244` [12] ④ — 「実装が要件を満たしていない → AI に実装を直させる」に、[6] ⑥ にある「**期待結果は `docs/TEST.md` の表の文言から変えないこと**」が無い。Reward Hacking の入口が [12] 側だけ開いている。【2】
- ⬜ **R12-48** `flow.html:1441` Section 3 冒頭 — 「詳細はページ末尾の『ラップアップ・発表会』を参照すること」にリンクが無い（`#wrap` は存在する）。【1】
- ⬜ **R12-49** `flow.html:2410` [14] — 「`.dev.vars` は Section 1 の設定で Read を拒否している」に、Section 1 の該当箇所（`#s06`）へのリンクが無い。【1】

## H. 検証必要性あり

- ⬜ **R12-50** `flow.html:1673` [9] ④ 手順1 — 「ローカルの `.dev.vars` に入れた値（**Secret key・Price ID など**）を本番側にも設定」。Price ID（`price_...`）は公開情報でシークレットではない。`wrangler secret put` で入れる必要があるのか、通常の環境変数でよいのかを要確認。
- ⬜ **R12-51** `flow.html:1611,1630` [9] ② — 「**月額・年額の2つ**の継続課金プランを作る」。Section 1 の要件（719行）は「サブスクリプション（月額／年額など）であることは必須。プラン数や金額は自由」。1630行の note でフォローはしているが、手順本文が2プラン固定の書き方になっている。参加者のプラン数と一致するか要確認。
- ⬜ **R12-52** `flow.html:1802,1815,2417` — SonarCloud / `/code-review` / `/security-review` / `security-guidance` が現行バージョンで記述どおりか。とくに 2417行の「インストールなしで最初から使えるものを『組み込みスキル』と呼ぶ」は用語として正しいか。
- ⬜ **R12-53** `flow.html:1860-1867` [11] — SonarCloud の Free プラン（$0）の条件と画面。プラン構成と画面は変わりやすい。図が8枚あるので、変わったときの改訂コストが大きい。
- ⬜ **R12-54** `flow.html:2017,2039` [12] — Clerk の「メール認証を一時的に ON にしてユーザを作り、また OFF に戻す」手順。Clerk 側の UI 変更で崩れやすい。また OFF に戻した後もテストユーザで認証が通り続けるかは、実地で確認した結果か要確認。
- ⬜ **R12-55** `flow.html:2035` [12] — 「メールは `e2e+clerk_test@example.com` のように `+clerk_test` を付ける。付けないと『Enter a valid email address』で弾かれる」。`+clerk_test` は Clerk のテスト用アドレスの規約だが、弾かれる理由が `@example.com` を使っているためではないかを要確認。
- ⬜ **R12-56** `flow.html:2365` [13] — 「`cloudflare-workers-and-pages[bot]` がリポジトリのルートに簡易的な `wrangler.jsonc` を自動生成して PR を送ってくることがある」。実地で起きた挙動か、現行でも再現するか要確認。
- ⬜ **R12-57** `flow.html:2438-2449` [14] — 5つの運用スキルを1つのプロンプトで作らせる設計。★☆☆（30〜45分）でこの分量が現実的か要確認。完了の定義が「最低1つ動作」になっているのは、実地で5つは無理だったからではないか。

## 確認してほしい判断（R12）

- **[12] の判定軸（3択）を [6]（4択）に揃えるかどうか（R12-11）は、直すと工程が変わるので手を付けていない。** E2E では「仕様が未決」が出るという [12] 固有の事情があるので、3択のままが正しい可能性がある。
- **用語統一（R12-24）の範囲。** Section 3 の「キー」34件を「購入の証」に揃えると、コード例の識別子（`licenseMasked` / `license_masked`）や画面の表示例（`GMC-••••-••••-FS9J`）にも及ぶ。識別子は実装の名前なので変えない方がよいと考えているが、そうすると本文と例で語が割れる。
