# flow.html レビュー ピックアップ

## A. 誤り・矛盾（直さないと参加者が詰まる）

* そもそも何を言っているかわからない。（）で情報を付加しているつもりなのでしょうが、同じ行と判断されかねないかと。あとそもそも確かめる必要のない項目だったはテストの書き方が悪いのではなく、「不必要な設計が行われていた」では？そもそもプロンプトにおいてもこれらは判別させていないため、急に表に現れた未知の何か
* あと「⑥ テスト結果 で処理済み（④ テスト項目作成 からやり直す）」これは日本語的に本当に意味がわからない。

**| ⑥ の判定                                                              | 直す対象                                                   |
| ------------------------------------------------------------------------ | ------------------------------------------------------------ |
| 実装が要件を満たしていない                                             | 実装を直させる（テストファイルは変更禁止）                 |
| テストの書き方が悪い（前提条件を作っていない、要素の指定が違う、など） | テストを直させる（実装は変更禁止）                         |
| テストの書き方が悪い（そもそも確かめる必要のない項目だった）           | 直さない。結果列に判定と理由を書いて閉じる                 |
| 要件そのものが間違っている                                             | ⑥ テスト結果 で処理済み（④ テスト項目作成 からやり直す） |

**


## B. 分かりにくい・誤読を招く

* 「購入したキーが消えない」なら、破られたときに「金を受け取ってモノを渡していない状態になる」と言える。これは載せる。落ちるのは次のような候補である。」言えるため、これは載せる。と一文で
* 挙げるのは、自分が Section 1 と 2 で作った画面、API、DB の3層のどこかである。」「通し」を含めた4項目で見る必要があるため、そもそもこの行はミスリード。消しておくくらいがいいかも
*

## C. 表記・体裁・掃除

## D. HTML / JS の実装

## E. 改行判断

## F. 削除要請(Cでの削除対応が増えたため)

* 載せると決めた項目が、そのまま並んでいる。→冗長。消す
* テスト設計を行うと、このような表となる（玄米茶サンプル。自分のアプリでは中身が変わる。ここでは要件2つ分だけ抜粋しており、実際は ① テスト要件 で書いた要件すべてについて行が続く）。」テスト設計表はトグルで閉じられているため、この行を表で書くと資料全体が重くなる。自明のことが多いため、まとめて消してもいいかも
* 要件を壊れる場所ごとに分けて行にしてあるので、落ちた行がそのまま原因の場所を指す。T-001 が落ちたなら画面、T-003 が落ちたなら保存処理である。」トグル引用をやめてくれ。消して

## G. 追記要請

## H. 検証必要性あり

* ここから実装のセッションに戻る。この ③ テスト環境準備 以降は実装コードを読ませてよい。」戻る必要はあるのか？
* **出力の skipped が0件であることを、人間が自分で見る。** skipped があってもテスト全体は成功として扱われるので、画面は緑のまま、確かめた本数だけが足りなくなる。」そもそもテスト項目作成段階でskipさせないようにする旨をプロンプトにぶち込めばいいのではと言う検証が必要そう

## 確認してほしい判断（今回こちらで決めたもの）

### [6] 機能テスト（今回分）

* **⑥ の突き合わせプロンプトに「通しの行を突き合わせから外す」を戻した。** 前ラウンドで「無駄」として消した指示なので、判断が反転している。あわせて下の箇条書きから「（「通し」の行はここに出るのが正しい）」を削除した
* **`git diff` の `--stat` を外した。** 指摘のとおり、1ファイルに複数テストがある場合はファイル単位の増減しか出ず、⑦ で要求している「期待結果の値が変わっていないか」を見られない。⑦ 手順3 の末尾も「`git diff` で中身も見る」→「変更の中身まで見る」に整理（コマンドが1つになったため）
* **「要件を数え上げきる」は「テスト要件で守るべきことを漏れなく列挙しきる」に。** 提案の「全ての要件をテスト化することは目標にしない」は要件→テスト項目の網羅性の話で、①（要件の列挙）とは別の主張になる。しかも [6] は「結果列を埋めきる」を完了条件にしているので、そう書くと矛盾する

### [6] ⑦ コード修正（前回分）

* **⑥ の3判定のうち「テストの書き方が悪い」に処理が無かったので追加した。** 手順1 を判定ごとの表にし、「テストを直させる（実装は変更禁止）」の行を足した。あわせて次を調整
  * テストを直させるプロンプトに「期待結果は `docs/TEST.md` の表の文言から変えないこと」を入れた。これが無いとテスト修正の許可がそのまま Reward Hacking の入口になる
  * 手順3 の git 差分チェックを「差分が出たら NG」→「**出た場所が自分で指示したテストだけか**を見る。テストを直させた行は中身も見て、期待結果の値が変わっていたら差し戻す」に変更
  * ポイントの「テストの書き方が悪い行は緑にしようがない」を「**そもそも確かめる必要のない項目だった**行」に限定
  * これで [13] E2E ④ の判定表（「テストの書き方が悪い → AI にテストを直させる」）と扱いが揃った
* **⑦ 末尾の重複コミットを削除した。** ⑧「壊して赤」を消したときにコミットだけ残してしまい、直後の「完了したら push する」と同じ作業が2回並んでいた

### Section 3（前回分）

* **ポートの固定表記を直した。** 指摘のとおり `wrangler dev` のポートは環境で変わるので、[13] ⓪ 準備 に「`npm run dev` の出力に表示されたものに合わせる（既定は 8787 だが、使用中なら別のポートが割り当てられる）。以降このページに出てくる 8787 も自分の環境のポートに読み替えること」と1回だけ書いた。**本文中の `8787` 自体は書き換えていない** — codegen のコマンド例など具体値がないと形が示せない箇所が多く、全部を `<ポート>` に置き換えると読みにくくなるため
* **[9] 決済のファイル指定を全部落とした。** 見出しの「実装ファイル（ここに作る）」、② の `stripe.ts` / `license.ts`、③ の前提文の3箇所。③ の前提は「② の実装が完了してから進む」に置き換えた
* **[10] Clerk の `GET /api/me` を「API」に置き換えた。** Section 2 でパス固定の記述を消したので、ここだけ残っていた
* **SonarCloud の重複**は、外側の「誤って All repositories を選んでしまった場合は、次のどちらかで対処する」を残し、内側の「すでに「All repositories」で許可してしまった場合は、あとから絞れる」を消して入れ子を1段浅くした

### Section 1 まで

* **「Section 2 における環境確認タイム」→ Section 1 のものと解釈した。** 前準備の直後（`ps-overview`）へ「環境確認」として移し、**Section 1 はセキュリティ対策から始まる**形にした
* **料金プランの記述**は「LP に料金プランを載せる。プラン数や金額は自由だが、サブスクリプション（月額／年額など）であることは必須」に書き換え、見出しも「推奨に従うもの」→「要件として外せないもの」に変更
* **玄米茶サンプルの説明文**は、サンプル画像5枚の導入文が消えるため「資料中の例「玄米茶」は説明用の共通サンプルである。」の一文だけ残した
* **Section 1 クロージングの削除**は節のみ。タイムテーブルの「1:50–2:00 クロージング」の行は時間枠として残した

### Section 2（番号ずらし）

* **[5] レビュー／[6] 自動テスト に分け、以降を [7]〜[14] へ繰り上げた。** `id`・ゴール表・タイムテーブル・目次・Section 3 の相互参照・範囲表記まで追従済み。`img/workshop_flow_diagram.svg` は Core 行を 6box → 7box に組み直した
* **「壊して赤になるか確認する」は [6] だけでなく [13] E2E テストの ⑤ も削除した。** **E2E 側は残す意図だった場合は戻す**

### 文体・体裁（今回分）

* **「だ・である調」への統一を全体に通した。** ps-overview / Section 1 / Section 2 / **Section 3 / 付録** まで適用済み
  * です・ます調は Section 3・付録で4箇所あり、すべて直した（発表会の案内文、Resend の API キー方針、Clerk のログイン方式、付録の見出し）
  * 体言止めは**本文（`<p>` と説明文の `<li>`）だけ**を「〜である」に直した。**表のセル・見出し・`summary`・手順ラベル（「お題:」「学び:」「ツール:」等）・箇条書きの列挙は体言止めのまま**にしてある。全部直すと冗長になるため
  * 残っている「です・ます」は2箇所あるが、いずれも**プロンプト例の中の文字列**（「このセクションは不要です」）と**画像の alt テキスト**なので、資料の文体ではないと判断して触っていない
* **[8][9][10] 共通の見出し**を「[8][9][10] 共通：シークレット管理」に変更した（目次も追従）。節の中を指す参照文（「共通の手順参照」「共通の秘密情報の置き場所参照」）はリンク先が変わらないのでそのままにしてある
* **⑦ コード修正 の結果列の表も `<details>` に畳んだ**（「結果列を埋めた表の例を見る」）。導入文は「結果列を埋めると、次のような表となる」に変更

### Section 2（前回分）

* **⑥ テスト結果 の突き合わせプロンプトを簡素化した。** 「grep で抽出して diff で」と「通しの行を外して別に列挙して」の2点を削除し、番号の突き合わせを頼むだけにした。あわせて下の箇条書きの「外した番号 →」の行も落とし、「表にあってコードにない」の説明に「通しの行はここに出るのが正しい」を吸収させた
* **⑦ コード修正 のポイントを「結果列を埋めきること」に書き換えた。** 「テストの書き方が悪い」と判定された行は緑にできないので、判定と理由を結果列に書いて閉じる、という形にした。完了の定義も「「通し」以外の行が全部緑」→「緑になっているか、緑にしない／できない理由が結果列に書かれている」に変更
* **丸番号の参照を「丸番号 セクション名」に統一したのは [6] 自動テストの中だけ。** [13] E2E テストの ①〜⑤ は工程名を付けていないので手を付けていない。必要なら次のラウンドで
* **③ テスト環境準備 の「「関数」の行しかなければ…」の段落は丸ごと削除した。** 代わりに「やること」に「（通しについては今は除外）」を入れてある

## 検証ログ

### R2（見た目レビュー）

* ✅ 全5ページ（概要 / Section 1 / Section 2 / Section 3 / 付録）をヘッドレス Chrome で幅 1440px・実寸レンダリングし、全高をタイル分割して目視した
* ✅ レイアウト崩れ（要素の重なり・はみ出し・横スクロール）は検出なし。テーマ配色・toggle・コピーボタン・目次ハイライトも見た目上問題なし
* ⚠️ 撮影時、高さ 5 万 px 超のキャンバスで Chrome ヘッドレスの描画が壊れて白帯が出た（[14] 付近）。短縮ページで撮り直して実レイアウトは正常と確認済み。資料側の問題ではない

### 過去分

* ✅ アンカー切れ: 0 件
* ✅ 画像の参照切れ: 0 件
* ✅ ラベルとアンカーの整合: 不一致 0 件
* ✅ 文体スキャン（全体）: 資料本文の です・ます 0 件
* ✅ ブラウザ確認: 見出し・工程名・toggle が意図どおり。コンソールエラーなし（スクリーンショットは環境側の不具合で撮れず、DOM で確認した）
* ⬜ フロー図の**見た目**の確認: Core 行を組み直したので、7 box が枠内に収まっているか目視してほしい
* ⬜ 外部リンクの生存（デモ URL・各サービスのログイン URL）: 未確認

---

# R11: Section 2 レビュー（[4]〜[7] / flow.html 865〜1457行）

機械チェック（アンカー・画像・ラベル整合）は全て `none`。以下は内容の指摘。

## A. 誤り・矛盾（直さないと参加者が詰まる）

- ⬜ **R11-01** `flow.html:1389` [6] 完了の定義 — 「落ちた項目を直したとき、テストファイルに差分が入っていない」が、⑥ 手順2 の「テストの書き方が悪い → テストを直させる」と正面から矛盾する。⑥ 手順4（1356行）は既に「差分が出た／出ないではなく、出た場所が自分で指示したテストだけか」に更新済みなのに、完了の定義だけ旧基準のまま残っている。
  - → 対応: 「テストファイルの差分が、自分で指示したテストの範囲に収まっている（期待結果の値が変わっていない）」に書き換える
- ⬜ **R11-02** `flow.html:1002` [4] ポイント — 「Section 2 のログイン周りはモック認証でよい（手順3）」の参照先 `#s4-step3` は「API をまとめて実装させる」で、モック認証の記述がない。さらに 926〜933行のプロンプトにも「モック認証」「メールアドレスをヘッダで受け取る」の指示が一切ない。参加者はモック認証をどこで作るのか分からない。
  - → 対応: 手順3 のプロンプトにモック認証の実装指示を足すか、独立した手順にする。参照先も直す
- ⬜ **R11-03** `flow.html:1259` [6] ⑤ テスト結果 やること — 「テストを実行させ、表とコードを突き合わせ、落ちた原因を切り分ける」と宣言しているが、手順1 は突き合わせ、手順2 は切り分けで、**テストを実行する手順が無い**。手順2 は「赤が出たときだけ」と言うが、実行しないと赤かどうか分からない。
  - → 対応: 手順1 の前に実行の手順（またはプロンプト）を足す
- ⬜ **R11-04** `flow.html:1402,1405` [7] 手順1 — 「binding＝…（[4] 手順2 で wrangler.jsonc に書き込んだ `database_id` のこと）」は誤り。binding は Worker から参照する名前（`DB`）、`database_id` は D1 の識別子で別物。プロンプトの「D1 の binding id が実際に作成したリソースと一致しているか」も両者を混ぜている。
  - → 対応: binding 名と database_id を分けて書く
- ⬜ **R11-05** `flow.html:950,981,995` [4] 手順4 / npm scripts 表 — `npm run db:migrate:local` を手順4 で実行させるが、scripts の追加は後の「必要な npm scripts」節（971行〜）。同様に `build:front` も手順5 より後で定義される。手順の順に進むと未定義の script を叩くことになる。
  - → 対応: npm scripts 節を [4] の流れの前（または手順1 の直後）に移す
- ⬜ **R11-06** `flow.html:981` npm scripts 表 — `test: vitest run` を [4] の時点で追加させているが、vitest の導入は [6] ③ テスト環境準備（1228行〜）。この時点では動かない。
  - → 対応: `test` の行を [6] ③ に移すか、表に「[6] で入る」と注記する
- ⬜ **R11-07** `flow.html:1020` [4] 完了の定義 — Console から `fetch('/api/contact', ...)` を叩かせているが、917行で「API のパス・入出力の設計は AI に任せる」と宣言している。参加者の API パスが `/api/contact` である保証がない。
  - → 対応: 「問い合わせ API のパス（AI が決めたもの）」と書くか、この確認自体を落とす
- ⬜ **R11-08** `flow.html:978,979` npm scripts 表 — `wrangler d1 migrations apply DB --local` の `DB` は binding 名。AI が作る `wrangler.jsonc` の binding 名が `DB` である保証がない。
  - → 対応: 手順2 のプロンプトに binding 名を `DB` にする指示を足す

## B. 分かりにくい・誤読を招く

- ⬜ **R11-09** `flow.html:1077-1082` [6] 流れの表 — ⑥ の説明が「フロント／バックエンドの不具合の場合、AI でコードを修正して再度テストを実施する」だけで、実態（コミット→判定別の修正→結果列記入→git diff→自分で実行の5手順）と合っていない。他3判定（テストの書き方が悪い／テスト設計が誤っている／要件が間違っている）の扱いが表から読めない。
  - → 対応: ⑥ の説明を「判定ごとに対処し、結果を表に書き残す」に広げる
- ⬜ **R11-10** `flow.html:1276` [6] ⑤ 手順2 — 「全部緑ならこの手順と次の修正は飛ばして、結果列の記入へ進む」。⑥ 手順1（コミット）・手順4（git diff）・手順5（自分で実行）も飛ばすのかが読めない。
  - → 対応: 飛ばすのは ⑥ 手順2 だけ、と明示する
- ⬜ **R11-11** `flow.html:1161 vs 1203` [6] ② テスト設計 — プロンプトで「関数 / API / 画面 / 通し のどれか」と指定しているが、4語の意味を説明する表は 1203行、プロンプトより後にある。実行時点で参加者は語の意味を知らない。
  - → 対応: 4種の表を ② の冒頭（プロンプトの前）へ移す
- ⬜ **R11-12** `flow.html:992 vs 1008,1361` [4]/[6] — 「自分でコマンドを打たず、Claude Code に依頼して実行させるのが基本である」と書きながら、完了の定義（1008行）と ⑥ 手順5（1361行）は自分で打つ前提。⑥ 手順5 は意図的な例外だが、その旨が書かれていない。
  - → 対応: ⑥ 手順5 に「ここは例外で、必ず自分で打つ」と一言添える
- ⬜ **R11-13** `flow.html:1341-1347` [6] ⑥ 結果列の例 — T-002/003/005/007/009/008 の順で **T-009 が T-008 より前**。また ② の表（1181-1186）にあった T-001/004/006 が消え、T-007〜T-009 が初出で、対応関係が追えない。
  - → 対応: 番号順に並べ替える。② の表と同じ番号を少なくとも1つ残す
- ⬜ **R11-14** `flow.html:1145 vs 1196` [6] ① ポイント / ② 手順2 — ① は「漏れなく列挙しきることは目標にしない」、② は「観点の抜けを人間が見る」。矛盾ではないが並べて読むと引っかかる。
  - → 対応: ② 側に「① で挙げた要件の範囲内で抜けを見る」と限定を足す
- ⬜ **R11-15** `flow.html:1301` [6] ⑥ 手順1 — 「git に追跡されていないファイルは差分が出ないので」の「追跡されていないファイル」が唐突。新規テストファイルの話だと分かるまで一拍かかる。
  - → 対応: 「④ で新しく作られたテストファイルはまだ git に追跡されておらず」と具体化する
- ⬜ **R11-16** `flow.html:1039` [5] コマンド一覧 — 「security-guidance … 自分で実行する必要はない」を、実行するコマンドの箇条書きに並べている。リストの意味が揃っていない。
  - → 対応: `<ul>` から出して、下の note に移す
- ⬜ **R11-17** `flow.html:1008` [4] 完了の定義 — 「表示・遷移・状態管理などはブラウザでしか確認できない」の「状態管理」が、下の4項目のどれとも対応していない。
  - → 対応: 「状態管理」を落とすか、対応する確認項目を足す
- ⬜ **R11-18** `flow.html:912` [4] 手順2 — 「ローカルとクラウドのデータは別物（詳細は手順4）」だが、手順4（937行）にも同じ抽象度の一文があるだけで「詳細」が無い。参照が空振りしている。
  - → 対応: 「詳細は手順4」を削るか、手順4 側に差が実際に効く場面を書く

## C. 表記・体裁・掃除

- ⬜ **R11-19** `flow.html:1108,1133,1134,1135` [6] ① — 「ライセンスキー」を使っているが、[4] 以降は「購入の証」に統一されている（Section 2 内で ライセンスキー4回 / 購入の証4回）。用語の一般化コミットの追従漏れ。
  - → 対応: [6] 内の「ライセンスキー」を「購入の証」に揃える。例の表（1181-1186, 1341-1347）も同様
- ⬜ **R11-20** `flow.html:1382` [6] 完了の定義 見出し — [4] は「[5] へ進む条件」、[5] は「[6] へ進む条件」なのに、[6] だけ「Section 2 の到達点・前半」。ラベルの形が揃っていない。
  - → 対応: 「[7] へ進む条件」に揃える
- ⬜ **R11-21** `flow.html:1030` [5] ゴール — 「…から push する。」だけ句点付き。他のゴール行（868/1066/1395）は句点なし。
  - → 対応: 句点を落とす
- ⬜ **R11-22** `flow.html:1361` [6] ⑥ 手順5 — `cd backend  &amp;&amp; npm test` の `backend` の後ろが半角スペース2つ。
  - → 対応: 1つにする
- ⬜ **R11-23** `flow.html:1418` [7] 手順4 — 「動作確認する 🎉」の絵文字が、[4][5][6] の手順に無い。
  - → 対応: 落とすか、他の到達点にも揃える
- ⬜ **R11-24** `flow.html:1395` [7] ゴール — 「（Section 2 のフィナーレ）」だけ語調が浮いており、[6] 完了の定義の「前半／後半」とも重なる。
  - → 対応: 落とす
- ⬜ **R11-25** `flow.html:905,966` [4] 手順2 / 手順6 — `<ul>` の中身が `<li>` 1つだけ。箇条書きの体裁になっていない。
  - → 対応: `<p>` にする
- ⬜ **R11-26** `flow.html:1192-1199` [6] ② 手順2 — 表の「順」列が 1, 2 だけで働いていない。
  - → 対応: 列を落として2項目の箇条書きにする
- ⬜ **R11-27** `flow.html:1429` [7] — `<h3 class="warn">⚠️ ハマりどころ` という見出しが [7] にだけあり、[4][5][6] には無い。構成が不揃い。
  - → 対応: 他の手順にも揃えるか、[7] のこの節を「ポイント」に統合する

## D. HTML / JS の実装

- ⬜ **R11-28** `flow.html:1042,1046` [5] figure — `alt` と `figcaption` がほぼ同内容で、`alt` が3〜4行と長い。スクリーンリーダーでは同じ説明を2回読み上げることになる。
  - → 対応: `alt` を1行に縮め、詳細は `figcaption` 側に寄せる
- ⬜ **R11-29** `flow.html:936-948` [4] 手順4 — `<blockquote>` の中に `<table>` と `<figure>` を入れ子にしている。引用ブロックとして重すぎ、手順の流れが分断される。
  - → 対応: `<details class="spec">` に畳むか、blockquote から出す
- ⬜ **R11-30** `flow.html:1177,1337` [6] ② / ⑥ — 表の例が `<details class="spec">` に畳まれているのは良いが、1177 の summary は「テスト項目の表の例を見る」、1337 は「結果列を埋めた表の例を見る」で、前者だけ「テスト項目の」と工程名が入っていない。
  - → 対応: summary の書式を揃える

## E. 文のつながり（旧「改行判断」）

- ⬜ **R11-31** `flow.html:1176` [6] ② — 「また、例えば「API がリクエストを処理している」ことのみしかテスト項目に挙げないで検証をすると、…ということになりかねない。」。「また、例えば」の二重接続、「ことのみしか」の重複否定、「挙げないで検証をすると」の不自然な接続。
  - → 対応: 「API がリクエストを処理していることだけを確かめると、テストは緑になるのに実際には動かない、ということが起こる。」
- ⬜ **R11-32** `flow.html:1203` [6] ② — 「**「どこで確かめる」列に書けるのは、次の4種である。**環境と必要なパッケージは ③ テスト環境準備 で入れる。」。無関係な2文を句点だけで連結している。後半は ③ の話。
  - → 対応: 後半を段落として分けるか、③ の冒頭へ移す
- ⬜ **R11-33** `flow.html:1224` [6] ③ — 「② テスト設計 で作った表の「どこで確かめる」列に出てきた種類（通しについては今は除外）だけ、実行環境を作らせる。」。括弧の挿入が主語と述語の間に入り、係り受けが切れている。
  - → 対応: 「…列に出てきた種類だけ、実行環境を作らせる。ただし「通し」は [12] で扱うのでここでは除外する。」
- ⬜ **R11-34** `flow.html:1238` [6] ③ — 「実行環境は**バックエンドは Workers、フロントは jsdom** を用いる」。「実行環境は」「バックエンドは」で主語が二重。
  - → 対応: 「バックエンドは Workers、フロントは jsdom を実行環境に用いる」
- ⬜ **R11-35** `flow.html:1336` [6] ⑥ 手順3 — 「書かせたあと、判定が自分の見立てと合っているかを人間が読む。結果列を埋めると、次のような表となる（② テスト設計 に載せた抜粋には無い行も含めた例）。」。無関係な2文が並び、後半の括弧が長い。
  - → 対応: 前半を手順の本文に残し、後半は details の直前に独立させる
- ⬜ **R11-36** `flow.html:1175` [6] ② — 「このテストのすべてが緑となる場合に「購入したキーが消えない」と推定することができる。」。「このテスト」の指示対象が曖昧（4項目全体か個々か）。「推定することができる」も冗長。
  - → 対応: 「この4行がすべて緑になって初めて「購入したキーが消えない」と言える。」
- ⬜ **R11-37** `flow.html:1285` [6] ⑤ — 「AI は…テストの期待値を緩めて緑にする道を選べる。」。可能形「選べる」が、ここでは「選びうる」の意味で使われていて読み手が引っかかる。
  - → 対応: 「…道を選ぶ」
- ⬜ **R11-38** `flow.html:1413` [7] 手順3 — 「frontend/ で wrangler deploy すると、フロントのみデプロイされ、バックエンドはデプロイされていないという事態になる:」。「という事態になる」が冗長で、コロンの前が締まっていない。
  - → 対応: 「…フロントだけが上がり、API が繋がらない。」
- ⬜ **R11-39** `flow.html:896-897` [4] 手順1 blockquote — 「name をフロントと同じにする理由: …」の1文目が3行にわたる複文で、条件・結果・逆条件が1文に詰まっている。
  - → 対応: 「同じ name なら Section 1 の URL を引き継ぐ／違う name なら別の Worker になる」の2文に割る

## F. 削除要請

- ⬜ **R11-40** `flow.html:918-924 と 930-933` [4] 手順3 — 必要な機能のリストが、本文の箇条書きとプロンプトの中で**完全に同じ内容で2回**出ている。
  - → 対応: 本文側の箇条書きを消し、プロンプトだけ残す
- ⬜ **R11-41** `flow.html:1069 と 1374` [6] — 「この工程の目的は、テストを緑にすることではなく…」と「この工程のゴールは『全部緑にすること』ではなく『結果列を埋めきること』である」が同じ主張の2回書き。
  - → 対応: 冒頭（1069）を残し、1374 のポイントは判定別の扱いだけに絞る
- ⬜ **R11-42** `flow.html:1443` [7] 完了の定義 — 「（余裕があれば）公開した本番 URL でトップページ・/mypage・API を1つブラウザで開き…」は、直前の3項目（1440-1442）と同じことを言っている。
  - → 対応: 丸ごと削除
- ⬜ **R11-43** `flow.html:956, 1401, 1013, 1441` — `assets.directory` / `assets.not_found_handling` / SPA フォールバックの説明が Section 2 内に4回出る（`not_found_handling` は Section 2 内4回でファイル全体の全数）。
  - → 対応: [7] 手順1 に一本化し、[4] 側は完了の定義の details だけ残す
- ⬜ **R11-44** `flow.html:980, 1416, 1431` [4]/[7] — 「`npm run deploy` はビルド → deploy まで一発」が3回。
  - → 対応: npm scripts 表の1回に絞る
- ⬜ **R11-45** `flow.html:1004 と 1425` [4] ポイント / [7] ポイント — 「Section 2 本編ではシークレット不要」が2回。
  - → 対応: [7] 側を削除
- ⬜ **R11-46** `flow.html:1003` [4] ポイント — 「秘密情報は .dev.vars / wrangler secret put で管理する。ハードコードや AI への指示文への貼り付けは禁止」は Section 1 のセキュリティ対策と重複し、しかも直後の 1004行が「Section 2 はシークレット無しで動く」と言っている。この節では使う場面がない。
  - → 対応: 削除し、[8][9][10] 共通のシークレット管理節（1482行〜）に寄せる
- ⬜ **R11-47** `flow.html:1292` [6] ⑤ ポイント — 「「通し」の行は、ここでは赤にも緑にもならない。④ で除外しているためである」は、1267行のプロンプト内で既に言っている。
  - → 対応: 削除
- ⬜ **R11-48** `flow.html:1367 と 1375` [6] ⑥ — 「赤が残っていてよいのは『未修理』と理由を書いた行だけ」と、ポイントの「時間内に直しきれない行が残ったら『未修理』と理由を書いて持ち帰る」が同内容。
  - → 対応: 手順5 側（1367）を削除
- ⬜ **R11-49** `flow.html:1424` [7] ポイント — 「（テスト → デプロイ の順）」は直前の文の言い換えで、情報が増えていない。
  - → 対応: 括弧ごと削除
- ⬜ **R11-50** `flow.html:1328` [6] ⑥ 手順3 — 「行数が多いので人が手で書かない。」は理由として弱く（行数は人による）、断定も強い。
  - → 対応: 削除するか「AI に埋めさせ、人間は読んで確かめる」に置き換える
- ⬜ **R11-51** `flow.html:887, 966, 997, 1010` [4] — 「表示された URL」の言い回しが Section 2 内で4回。うち 966 と 997 は同じ内容の隣接した注意書き。
  - → 対応: 997 の note に一本化する
- ⬜ **R11-52** `flow.html:1436 と 1450` [7] — 「必要なら dev を main にマージ」がプロンプトとクロージングで2回。
  - → 対応: クロージング側を削除
- ⬜ **R11-53** `flow.html:921` [4] 手順3 — 「（決済なし・Section 2 の簡易版。Section 3 [9] で Stripe 購入 → Webhook 発行に差し替え）」は括弧の中に2つの独立した補足が入っている。
  - → 対応: 「（決済なしの簡易版。[9] で差し替える）」まで縮める

## G. 追記要請

- ⬜ **R11-54** `flow.html:1098` [6] ① プロンプト — 「読むのは SCREEN_SPEC.md だけにする」だが、SCREEN_SPEC.md がどこで作られたものか（Section 1 [2]）へのリンクがない。
  - → 対応: 本文側に `<a href="#s2-spec">` を張る
- ⬜ **R11-55** `flow.html:1218 vs 1157-1166` [6] ② — ポイントに「同じことを確かめられるならテスト範囲が狭いほうを選ぶ」とあるが、② のプロンプトにその指示が入っていない。人間が後から直す前提になっている。
  - → 対応: プロンプトに「同じことを確かめられるなら、より狭い種類を選ぶ」を足す
- ⬜ **R11-56** `flow.html:1242-1255` [6] ④ — ④ だけ完了の判定がない（① は「この形になったら終わり」、② は「人間が確認する」、⑤⑥ は手順で閉じる）。
  - → 対応: 「表の行数とテスト本数が一致したら ⑤ へ」を足す
- ⬜ **R11-57** `flow.html:1313` [6] ⑥ 判定表 — 「要件そのものが間違っている → ④ に戻ってテストを作り直させる」だが、④ に戻ると ⑤ の突き合わせもやり直しになる。手戻りの範囲が書かれていない。
  - → 対応: 「④ → ⑤ をその行についてやり直す」と範囲を明示する
- ⬜ **R11-58** `flow.html:875, 1002` [4] — 「モック認証」を3回使っているが、実装形（`x-user-email` ヘッダ）の説明が Section 2 内に **0回**（ファイル全体では2回、Section 3 側にある）。[10] で差し替える対象が何なのか Section 2 だけ読むと分からない。
  - → 対応: 手順3 に一行、モック認証の実装形を書く

## H. 検証必要性あり

- ⬜ **R11-59** `flow.html:1226` [6] ③ — 「ここから実装のセッションに戻る」。① ② を新しいセッションで始めた後、元のセッションに戻る手順（`claude --resume` 等）が書かれていない。そもそも戻る必要があるのかも含めて要検証。※前ラウンドの H にも同じ指摘あり、未解決。
- ⬜ **R11-60** `flow.html:1252 vs 1363-1366` [6] ④ / ⑥ 手順5 — ④ のプロンプトで既に `test.skip / it.todo / it.only` を禁止しているのに、⑥ で skipped の検出方法に4段落を割いている。禁止指示が効くなら検出の記述は縮められるはず。※前ラウンドの H の「テスト項目作成段階で skip させない旨をプロンプトに入れれば」と同じ論点。
- ⬜ **R11-61** `flow.html:1231-1234` [6] ③ プロンプト — 「設定は cloudflareTest プラグイン方式で書く」。この名称が実在の API 名か、動作確認時点（2026-07）から変わっていないか要確認。同プロンプト内の `jsdom` / `testing-library/react` も未定義のまま出ている。
- ⬜ **R11-62** `flow.html:1143` [6] ① ポイント — 「モデルは高精度なもの（Fable / Opus）を使う」。モデル名は変わりやすい。また [1] の「事前確認: モデルと Effort の設定」と重複していないか要確認。
- ⬜ **R11-63** `flow.html:1035` [5] — 「既定の Medium で実行するのを推奨（Team standard などトークンに余裕がないプラン…）」。プラン名と Effort の既定値は変わりやすい。[1] の Effort 説明との重複も要確認。
- ⬜ **R11-64** `flow.html:1033,1037,1038` [5] — `/code-review` と `/security-review` を「Claude Code の組み込みコマンド」と書いている。現行バージョンで組み込みかプラグイン提供かを要確認。
- ⬜ **R11-65** `flow.html:1346` [6] ⑥ — 結果列の例で「通し」の行に「[12] で確認予定」と書かせているが、[12] E2E テストは Section 3 の**オプション課題**。[12] をやらない参加者の結果列がどうなるかが決まっていない。
- ⬜ **R11-66** `flow.html:1302-1303, 1355` [6] ⑥ — 手順1 のプロンプトで「私が言うまで追加のコミットはしないこと」と縛っているが、手順2 の修正時に AI が勝手にコミットしないかは運用依存。実際に破られた場合、手順4 の `git diff` が空振りする。

## 確認してほしい判断（R11）

- **「通し」の行の除外の重複は今回落としていない。** Section 2 内で「通し」の行の扱いが 1251 / 1267 / 1292 / 1333 / 1351 / 1388 の6回出る。R11-47 では 1292 だけを削除対象にした。前ラウンドで「通しの行を突き合わせから外す」を意図的に**戻した**判断があるため、残り5回はそのまま残している。まとめて整理するなら別途指示がほしい。
- **前ラウンドの A / B / F 節の項目は、本文を見る限り既に適用済みだった。** 「⑥ テスト結果 で処理済み（④ からやり直す）」は 1313行で「人間が要件と表の期待結果を直し、④ に戻って…」に、「購入したキーが消えない…」の分割文は 1122行で一文に、「テスト設計を行うと、このような表となる（玄米茶サンプル…）」と「要件を壊れる場所ごとに分けて…」の2件は削除済み。未削除のまま残っているだけと判断した。消してよければ言ってほしい。
